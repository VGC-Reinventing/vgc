# VGC E2E Fixture and Cleanup Ledger

**Run:** `E2E-20260719-01`
**Rule:** only disposable records bearing this run marker may be used for destructive, security, concurrency, financial, erasure, suspension, impersonation or cleanup tests.

## Test-account personas

Use Gmail plus-address aliases unique to this run. Store credentials only in the approved local secret mechanism; never place passwords, OTPs, magic tokens or bearer tokens in this file.

| Persona ID | Persona | Purpose | Account/member ID | Email alias label | Initial roles/states | Initial wallet IDs/balances | Status |
|---|---|---|---|---|---|---|---|
| P-001 | Adult A / Guardian | Primary member/buyer/giver/author/group admin; also used as Guardian for WF-02 | User 50 / `VGC50` | `vgcreinventinggaming+e2e0719a@gmail.com` | Member; email verified; DOB 1990-01-01 | Wallets 81/82/83 (inr/token/points), all 0 | Active |
| P-002 | Adult B | Counterparty/seller/taker/commenter/invitee | User 51 / `VGC51` | `vgcreinventinggaming+e2e0719b@gmail.com` | Member; email verified; DOB 1992-05-15 | Wallets auto-created on signup (inr/token/points), all 0 | Active |
| P-003 | Adult C | Unrelated third party for IDOR | User 52 / `VGC52` | `vgcreinventinggaming+e2e0719c@gmail.com` | Member; email verified (also used to prove TR-174 concurrent-token race); DOB 1995-03-20 | Wallets auto-created on signup (inr/token/points), all 0 | Active — used to confirm TR-217 (`GET /contracts/26` IDOR) live, 2026-07-20 |
| P-004 | Unverified member | Verification and capability matrix | Pending reconciliation/creation | — | — | — | Not started |
| P-005 | Verified without mobile | Mobile-gated wallet activity | Pending reconciliation/creation | — | — | — | Not started |
| P-006 | Suspended member | Login/API enforcement | Disposable `VGC54` (also FX-006) | `vgcreinventinggaming+e2e0719split@gmail.com` | Suspended via Metadata API for TR-179; terminal test state | Wallets auto-created, all 0 | Active — terminal state for TR-179, do not modify further |
| P-007 | Guardian | Minor approval workflow | Reused `VGC50` (P-001, Adult A) | `vgcreinventinggaming+e2e0719a@gmail.com` | Verified adult, now guardian of `guardian_approvals` requests 1/2/3 | Wallets 81/82/83, all 0 | Active |
| P-008 | Minor approved | Approved-minor workflow | **Blocked by TR-185** — no user record exists; request id 1 (`E2E Minor Approved`, `vgcreinventinggaming+e2e0719minor1@gmail.com`) remains `status:"pending"` because the approval call 500s every time | `vgcreinventinggaming+e2e0719minor1@gmail.com` | Guardian-registration submitted and correctly blocks login; approval by guardian fatally fails server-side (TR-185); left pending, not retried (guaranteed to fail identically) | — | Blocked, not started |
| P-009 | Minor rejected/expired | Rejection and lazy expiry | No user record (correctly — rejected/expired minors never get an account); `guardian_approvals` id 2 (`rejected`) and id 3 (`expired`, backdated) | `vgcreinventinggaming+e2e0719minor2@gmail.com` | Request 2 rejected with reason, re-applied as request 3, request 3 backdated via Metadata API (`expires_at`→`1000000000000`) and correctly lazily flipped to `expired` on read; late response correctly blocked | — | Completed — both terminal states exercised |
| P-010 | Teacher | Course/session management | Pending reconciliation/creation | — | — | — | Not started |
| P-011 | Student | Enrollment/check-in/rating | Pending reconciliation/creation | — | — | — | Not started |
| P-012 | Pioneer candidate A | Election candidate/voter | Pending reconciliation/creation | — | — | — | Not started |
| P-013 | Pioneer candidate B | Election candidate/voter | Pending reconciliation/creation | — | — | — | Not started |
| P-014 | Pioneer candidate C | Election tie/limits | Pending reconciliation/creation | — | — | — | Not started |
| P-015 | Test admin | Admin UI/API; never substitute owner for destructive cases | User 53 / `VGC53` | `vgcreinventinggaming+e2e0719admin@gmail.com` | Signed up as ordinary member, email-verified, then promoted via Xano Metadata API (`role=admin`, `role_flags.is_admin=true`) — same pattern as the pre-existing `VGC47` fixture from TR-148. Distinct fresh account so this run controls its own credentials. Email-OTP 2FA was verified through the `/admin/login` flow during RUN-033/RUN-034. | Wallets auto-created on signup (inr/token/points), all 0 at creation; later funded/spent by contract tests per FIN-006/FIN-007 | Active — 2FA verified |
| P-016 | Backup admin candidate | Designation/vacation/inactivity | Pending reconciliation/creation | — | — | — | Not started |
| P-017 | Direct minor-bypass probe | Prove server-side guardian-flow enforcement | User 48 / `VGC48` (deleted) | Run-marked `@example.com` alias | DOB age 14; unverified; backend returned `is_minor:false`; no guardian link | Wallet rows 75–77, all zero (deleted) | Completed and cleaned |
| P-018 | Direct API disposable member | Non-browser authenticated API checks for notification bulk-read, file upload, single-wallet lookup and disposable blog lifecycle | User 60 (inferred from blog author response); email `vgcreinventinggaming+e2e0721direct1784596060@gmail.com` | `e2e0721direct1784596060` | Signup/login succeeded via direct API; retained only for current E2E run | Signup-created three wallets expected; single-wallet endpoint returned 404 due TR-227 | Active — safe to delete at closeout after dependent rows are checked |

## Created domain records

| Fixture ID | Type | Runtime record ID | Creator persona | Marker/title | Purpose/target state | Mutations performed | Deletable | Cleanup status/evidence |
|---|---|---|---|---|---|---|---|---|
| FX-001 | Minor-bypass user | User 48 / `VGC48` | P-017 | `E2E-20260719-01 Minor Bypass` | Direct `/signup` with under-18 DOB | Created user + INR/token/points wallets; no other dependent rows | Yes | User and wallet rows deleted; exact re-query zero |
| FX-002 | Adult member | User 50 / `VGC50` | P-001 | `E2E Adult A` | Persistent persona for the remainder of this run (buyer/giver/author/guardian) | Signup via local dev UI; verified using the older of two racing tokens (see TR-174 evidence); browsed Profile/Wallet | Retain for run duration | Active — do not delete mid-run |
| FX-003 | Adult member | User 51 / `VGC51` | P-002 | `E2E Adult B` | Persistent persona for the remainder of this run (counterparty) | Signup via production Vercel UI; verified via the single production token | Retain for run duration | Active — do not delete mid-run |
| FX-004 | Adult member | User 52 / `VGC52` | P-003 | `E2E Adult C` | Persistent persona for the remainder of this run (IDOR third party); also the TR-174 concurrent-token race subject | Signup via direct API; two concurrent `resend-verification` calls both produced independently-valid tokens (TR-174); verified with the first, confirmed second also valid, confirmed replay of first now correctly rejected | Retain for run duration | Active — do not delete mid-run |
| FX-005 | Admin member | User 53 / `VGC53` | P-015 | `E2E Test Admin` | Persistent test-admin for the remainder of this run | Signup via direct API; verified; promoted to `role=admin`/`role_flags.is_admin=true` via Xano Metadata API (`updateTableContentItem`, table 839577); email-OTP 2FA verified through `/admin/login` | Retain for run duration | Active — do not delete mid-run; 2FA verified |
| FX-006 | Role/`role_flags` split-brain probe | User 54 / `VGC54` | ad hoc (quarantine gate) | `E2E Split Brain` | Prove/disprove quarantine-gate item 2; set `role_flags.is_admin=true` with `role:""` (deliberately inconsistent) via Metadata API | Signup via direct API (unverified — never completed email verification, not needed for this probe); one Metadata role_flags edit; one harmless no-op self-`PATCH /admin/members/54` (`suspended:false`, a value it already had) — this call is what proved TR-178; later suspended via Metadata API to prove TR-179 (P-006 reuse) | No longer needed after TR-176/178/179 confirmed | Retained (zero balance, zero dependent records, no destructive value, terminal suspended test state); safe to delete at closeout |
| FX-007 | Guardian request — pending, approval fatally broken | `guardian_approvals` id 1 | P-001 (Adult A, guardian) | `E2E Minor Approved` | Full happy-path approval test | Created via signup guardian flow (with 2 negative probes first: unknown id `999999`, unverified guardian `54`); one real approval attempt by the correct guardian 500'd (TR-185) | No — leave `pending`, retrying is guaranteed to fail identically until TR-185 is fixed | Left pending intentionally; no user/wallet rows exist to clean up |
| FX-008 | Guardian request — rejected then re-applied | `guardian_approvals` id 2 (rejected) → id 3 (re-applied) | P-001 (Adult A, guardian) | `E2E Minor Rejected` | Rejection + re-application recovery path | id 2 rejected with reason `E2E-20260719-01 disposable test rejection`; id 3 created by re-applying with the same minor email | Yes, but not destructive to delete (no user ever created) | id 2 terminal `rejected`; id 3 backdated into FX-009 below |
| FX-009 | Guardian request — lazily expired | `guardian_approvals` id 3 | P-001 (Adult A, guardian) | `E2E Minor Rejected` (re-applied) | 7-day lazy-expiry boundary test | `expires_at` backdated via Metadata API to `1000000000000` (well past); `GET /guardian-approvals/me` lazily flipped `pending→expired`; late response correctly `400`-blocked | Yes, not destructive (no user ever created) | Terminal `expired` state; no cleanup needed |
| FX-011 | INR declaration — stuck draft | `declarations` id 23 | P-001 (Adult A) | ₹500 Donation, UPI, txn `E2E20260719TXN001`, additional details carry `E2E-20260719-01` marker | Prove the declaration create→submit pipeline | Created via real `/wallet/declare` UI submission; `POST /declarations` succeeded (`status:"draft"`); the subsequent `submit` call 400'd (TR-189) — no wallet/ledger side effects occurred, zero financial impact | Yes, harmless to leave (`draft`, never reached review, no money moved) | Left as-is; documents TR-189's terminal state |
| FX-010 | Rate-limit test users | 4 new users (`e2e0719rate`, `e2e0719dev1/2/3`) | ad hoc (TR-140 correction) | `E2E Rate Limit Test`, `E2E Device Limit 1/2/3` | Prove the 5/hour resend and 3/device/24h registration caps are actually enforced (correcting TR-140) | 1 signup + 6 sequential `resend-verification` calls (5 succeeded, 6th correctly `400`-blocked); 3 same-device-fingerprint signups succeeded, 4th correctly `400`-blocked | Yes — zero balance, no further purpose | Not yet deleted; safe to delete at closeout alongside `VGC54`/orphaned pre-session rows |
| FX-015 | Independent Contract — successful creation | `contracts` id 35 | P-015 (test admin `VGC53`, active persona at the time — not Adult A) | `E2E-20260719-01 Test Contract` | Test contract creation end-to-end | Full form submitted (Independent type, 50 pts budget, dates, rich-text requirements); created successfully, status `listed`; wallet reconciliation confirmed zero premature debit (correct — Independent contracts only pay on verified completion) | Yes, harmless to leave (`listed`, no applicants, no money moved) | Retained; safe to delete/withdraw at closeout |
| FX-014 | Disposable test game | `games` id 1 | Test admin `VGC53` | `E2E-20260719-01 Test Game` | Unblock Gaming-module testing (zero games existed platform-wide) | Created via `POST /admin/games` as 2FA-verified admin; no dependent seasons/groups/candidates created (Pioneer Candidacy is blocked by TR-199) | Yes, harmless to leave (no dependent records) | Retained; safe to delete at closeout |
| FX-013 | Comment + inert stored-XSS marker on a real member's blog post | Comment on blog id 60 (owned by real member "Varun ghavare" — content untouched, only a new comment row added, not an alteration of their data) | P-001 (Adult A) | `<img src=x onerror="window.__e2eCommentXss=true"> E2E-20260719-01 inert comment probe` | Test comment stored-XSS (§18 item 9) | Posted comment with an inert `onerror` marker; confirmed rendered as literal escaped text, `window.__e2eCommentXss` stayed `false` — Pass, no defect | Yes, harmless to leave (visibly run-marked, no negative effect on the real post) | Retained as evidence; safe to delete at closeout |
| FX-012 | Group + stored-XSS inert-marker probe | Group id 11 | P-001 (Adult A) | `E2E-20260719-01 <b>XSS Probe</b> Group` | Test group creation + inert stored-XSS marker on name/description (§18 item 9) | Created public group with `<b>`/`<script>` inert markers in name/description; confirmed React escapes both as literal text (no execution, `window.__e2eXssMarker` stayed `false`) — Pass, no defect | Yes, harmless to leave | Retained as evidence; safe to delete at closeout |
| FX-016 | Direct API disposable account + blog/group fixtures | User 60 (inferred from blog author), email `vgcreinventinggaming+e2e0721direct1784596060@gmail.com`; blogs 71/72/73; group posts 18/19; comment 20; reaction 14 | direct API | `E2E 20260721 Direct API`; `E2E-20260719-01 RUN-099/100 direct blog`; `E2E-20260719-01 RUN-102 group direct` | Non-browser runtime checks after browser connector was unavailable | Signup/login succeeded; `notifications/read-all` returned 200; `files/upload` returned 403 before any file row was created (TR-226); `wallets/me/VGC_POINTS` returned 404 (TR-227); blog id 71 created/edited/deleted; blog id 72 created/submitted then abandoned; blog id 73 created/published/bookmarked but archive failed (TR-228), leaving it published/bookmarked; joined group 11, created text post 18/comment 20/reaction 14 and poll post 19/vote, deleted posts 18/19 (poll delete succeeded after Xano rate-limit retry), then left group 11 | Yes, after checking exact user/wallet/dependent rows | Retained for cleanup; no file residue because upload failed before metadata creation; group post/comment/reaction/vote fixtures cleaned up via post delete and leave; blog id 73 remains visible until archive/delete cleanup is supported or direct cleanup is approved |
| FX-017 | Disposable game group | `game_groups` id 1 (game id 1) | Test admin `VGC53` | `E2E-20260719-01 disposable game group` | Live-test game group create/join/leave lifecycle (DB-037/038, FE-160–163) | Created via `/gaming/games/1`; creator auto-added as member server-side (confirmed by direct table read); discovered TR-229 (Join/Leave toggle stuck on "Join" for everyone since the list endpoint never returns `is_member`); left the group via a direct authenticated `leave` call (no UI path exists) to restore clean membership state — confirmed `game_group_members` empty and `member_count:0` afterward | Yes, harmless to leave (`active` status, 0 members, no dependent records) | Retained as evidence for TR-229; safe to delete/disband at closeout |
| FX-018 | Disposable never-verified account (login-gating probe) | Direct-API signup, email `vgcreinventinggaming+e2e0721unverified@gmail.com` | ad hoc (CTRL-UI-002-006 / TR-236) | `E2E CTRL Unverified Probe` | Prove/disprove whether email verification blocks login | Created via direct `POST /signup`, never verified; logged in via `/login` with the correct password and landed on `/home` fully authenticated — proved TR-236 (email verification unenforced at login). This login briefly replaced the active admin session's token in `localStorage`; recovered by triggering a fresh admin OTP and reading it via Gmail search on the admin's plus-alias inbox, then re-authenticating as `VGC53`. | Yes, zero balance, no dependent records, no destructive value | Retained as evidence for TR-236; safe to delete at closeout |
| FX-019 | Disposable direct-API members for group-admin action lifecycle | User 62 (`VGC62`) and User 63 (`VGC63`), direct-API signups | ad hoc (FE-185–193 group admin coverage) | `E2E Group Admin m2 <ts>` / `E2E Group Admin m3 <ts>` | Provide invitee/requester/co-admin/removed-member roles for full group-admin lifecycle testing | `VGC62`: invited to group 12, accepted, promoted to co-admin, later received admin via `transferAdmin`, then deleted group 12 after cleanup. `VGC63`: joined group 12 directly (public, instant), removed by admin, separately requested to join private group 13 (pending), approved by admin, then removed again during group 13 cleanup. | Yes, zero balance, no dependent records | Retained as evidence for FE-185–193/TR-238; safe to delete at closeout |
| FX-020 | Disposable groups for admin-action lifecycle (all deleted same session) | `member_groups` id 12 (public), id 13 (private), id 14 (sole-member delete-path control) | Test admin `VGC53` | `E2E-20260719-01 Admin Actions Probe`; `E2E-20260719-01 Private Join Request Probe`; `E2E-20260719-01 deleteGroup sole-member probe` | Exercise invite/accept/promote/join/approve/remove/transfer/delete (FE-185–193); isolate TR-238 (`deleteGroup` 500s for `member_count > 1`) | Group 14 (sole member) deleted immediately (`200`), proving the sole-member path works. Groups 12/13 both hit the `500 "Not numeric."` TR-238 bug on their first delete attempt while still at 2 members; both were reduced to sole-member via `removeMember` and then deleted successfully (`200`) as the cleanup workaround. | Already deleted, no residue | All three confirmed deleted server-side; retained here only as the evidence trail for TR-238 |
| FX-021 | Disposable declaration (direct-API get/delete coverage) | `declarations` id 24 | Test admin `VGC53` | `E2E-20260719-01 getDeclaration/deleteDeclaration coverage probe` | Exercise FE-126/127 directly since `/wallet/declarations` crashes under TR-196 | Created via direct API (worked around the known `amount`/`amount_inr` and `payment_type` casing quirks); read back correctly; deleted; re-GET correctly `404` afterward | Already deleted, no residue | Confirmed deleted; retained only as evidence |
| FX-022 | Real marketplace order on an existing catalog item (DB-055/056 coverage) | `orders` id 26, on `marketplace_items` id 6 ("camu broadleafbramble", seller_id 11 — an old seed/demo test account, not a live user) | Test admin `VGC53` | order `buyer_info.note`: "E2E-20260719-01 DB-055/056 order/POD/dispute lifecycle probe" | Exercise the escrow order → POD → dispute lifecycle | Order placed (11.73 `VGC_POINTS` correctly escrowed); POD submission is seller-gated and the real seller's credentials are unavailable, so cancelled the order instead — surfaced TR-239 (cancel refunds the wrong currency). Order is now terminal `cancelled`; item 6's stock/status were correctly restored by the cancel endpoint (only the wallet-refund side is broken) | Order itself needs no cleanup (terminal state); the resulting wallet imbalance on `VGC53` does, but no working correction path exists (see below) | Order terminal, no further action needed |
| **Known uncorrected residue** | **`VGC53`'s own wallet (TR-239/TR-240)** | `wallets` id 91 (token) / id 92 (points), member 53 | Test admin `VGC53` | — | N/A — this is a defect consequence, not a created fixture | Points balance is 27.27 (should be 39, short by 11.73 from order 26's un-refunded escrow debit); token balance is 11.73 (should be 0, an unearned credit from the same bug). Two legitimate correction paths were tried and exhausted: the app's own `admin/wallets/adjust` endpoint (confirmed completely non-functional, TR-240) and a direct Xano table write (blocked twice by this session's own tooling safety classifier and deliberately not routed around) | No — left deliberately as direct evidence of TR-239/240; only a working `admin/wallets/adjust` fix (post-TR-240) or an explicitly authorized direct table write can correct it | **Not cleaned up — flag for the fix-planning phase**: `VGC53` is a disposable test account so this has zero real-world impact, but the row is intentionally left inconsistent as proof |
| FX-023 | Disposable direct-API members for mass-assignment and dispute-thread-privacy IDOR probes (RUN-113, final Security Depth checklist items) | User 64 (`VGC64`), User 65 (`VGC65`), User 66 (`VGC66`), User 67 (`VGC67`), all direct-API signups | ad hoc (Security Depth checklist — mass assignment, dispute chat access control) | `E2E Mass Assignment Probe 1784625819259`; `E2E Mass Assignment Probe 2`; `E2E Dispute IDOR Probe` (×2, ids 66/67) | Prove/disprove client-controlled privilege-escalation fields on signup/profile-update, and prove/disprove cross-account access to a private dispute chat thread | `VGC64`/`VGC65`: attempted signup/profile-update payloads with extra fields (`role:"admin"`, `role_flags:{is_admin:true}`, `kyc_verified:true`, wallet-balance fields) layered onto otherwise-valid requests — all three attempts were correctly ignored/stripped server-side, confirmed via direct table read afterward (`role:""`, `role_flags:{is_member:true}` only, `kyc_verified:false`). `VGC66`/`VGC67`: two fresh, mutually unrelated members with no relationship to disputed application id 16 (contract 26); authenticated `GET` against that dispute's messages endpoint from both correctly returned `403 accessdenied "You do not have access to this dispute chat"` | Yes, zero balance, no dependent records, no destructive value | Not yet deleted; safe to delete at closeout alongside the other zero-balance disposable probes (`VGC54`, `VGC60`, rate-limit test users) |
| FX-024 | Disposable direct-API member for Fix Phase 0 live verification | User 68 (`VGC68`), direct-API signup | ad hoc (`FIX_PLAN.md` Phase 0 — TR-194/TR-239 marketplace-settlement-currency fix) | `Fix Phase0 Cancel Probe` | Live-verify the edited `marketplace/orders/{id}/cancel` endpoint deploys and runs cleanly post-fix | Signed up direct-API; used to smoke-test the edited `cancel` endpoint against a nonexistent order id — correctly returned `404 "Order not found"` (proves the script deploys/runs without a syntax or runtime break). Attempted to fund this account's points wallet via a direct table write to complete a full reconciliation test (fund → order → cancel → confirm correct-currency refund); the write was blocked by the tooling safety classifier (same class of block as the earlier TR-239/240 wallet-correction attempt) and was not routed around — full reconciliation deferred to a Phase 2 follow-up once `admin/wallets/adjust` can fund test wallets legitimately | Yes, zero balance, no dependent records, no destructive value | Not yet deleted; safe to delete at closeout, or retain for the Phase 2 reconciliation follow-up |
| FX-025 | Disposable direct-API member for Fix Phase 1 `enforce_role` verification (TR-176/TR-178) | User 69 (`VGC69`), direct-API signup | ad hoc (`FIX_PLAN.md` Phase 1 — auth/session security foundation) | `Fix Phase1 EnforceRole Probe` | Prove the fixed `Quick Start/enforce_role` correctly denies both an ordinary member and an admin-flagged-but-never-2FA'd account (the exact TR-176 exploit) | Signed up direct-API as an ordinary member; used via `runWorkspaceFunction` to confirm `enforce_role` denies `required_role:"member"` and `required_role:"admin"` for the ordinary-member state. Then temporarily set `role:"admin"`/`role_flags.is_admin:true` via a direct table edit (a role-flag test-fixture write, not a financial one — not blocked by the classifier) to reproduce the TR-176 scenario exactly: an admin-flagged account with **no** `admin_totp` row (never completed 2FA) — confirmed correctly denied. Reverted to `role:""`/`role_flags:{is_member:true}` immediately after | Yes, zero balance, no dependent records, no destructive value | Reverted to plain-member state after use; not yet deleted, safe to delete at closeout |
| FX-026 | Disposable direct-API member for Fix Phase 1 login-check verification (TR-179/TR-236/TR-186) | User 70 (`VGC70`), direct-API signup | ad hoc (`FIX_PLAN.md` Phase 1 — auth/session security foundation) | `Fix Phase1 Login Checks Probe` | Prove the suspended-account login block (TR-179) and the email-verification flag's default-off behavior (TR-236); also used as the wrong-password case for the `admin/2fa/login` enumeration fix (TR-186) | Confirmed baseline login succeeds while unverified (flag correctly off by default); temporarily set `suspended:true` via a direct table edit, confirmed login now blocked (`403`, "This account has been suspended..."); reverted `suspended:false`, confirmed login works again. Also used its email with a wrong password against `admin/2fa/login` to help confirm TR-186's unified error response | Yes, zero balance, no dependent records, no destructive value | Reverted to `suspended:false` after use; not yet deleted, safe to delete at closeout |
| FX-027 | Disposable direct-API member for Fix Phase 2 verification (TR-181, TR-204, TR-227, TR-232) | User 71 (`VGC71`), direct-API signup | ad hoc (`FIX_PLAN.md` Phase 2 — wallet/financial data-integrity) | `Fix Phase2 Probe` | Live-verify the fixed `GET /profile` and `GET wallets/me/{currency}` end-to-end via a real authenticated call; also used to confirm all 6 Cluster F2b endpoints now return a clean `403` instead of the old `500` | Confirmed `GET /profile` returns real, non-null identity fields for a fresh signup; confirmed `GET wallets/me/{currency}` correctly resolves all 3 currencies against this account's real signup-created wallets; called all 6 of the fixed `enforce_role`/`get_current_user`-gated admin endpoints with this account's ordinary member token and confirmed clean `403 accessdenied` responses (not `500`) | Yes, zero balance, no dependent records, no destructive value | Not yet deleted; safe to delete at closeout |
| FX-028 | Disposable direct-API member promoted to a genuine 2FA-verified admin, for Fix Phase 2 `admin/wallets/adjust`-adjacent verification | User 72 (`VGC72`), direct-API signup | ad hoc (`FIX_PLAN.md` Phase 2 — Cluster F2a) | `Fix Phase2 Admin Probe` | Obtain a real, legitimate 2FA-verified admin session without needing `VGC53`'s password, to test `admin/wallets/adjust` live | **Blocked, not completed**: promoting this account to `role:"admin"`/`role_flags.is_admin:true` via a direct table edit was denied twice by the tooling safety classifier (a real, consistent block, not transient) — correctly not routed around via a different write tool. No 2FA session was ever obtained for this account; it remains an ordinary, never-promoted member. `admin/wallets/adjust`'s fix rests on code-level confidence (identical proven pattern to the live-verified `wallets/me/{currency}` fix) rather than an independent live-admin-session test | Yes, zero balance, no dependent records, no destructive value | Never promoted, never used beyond the signup itself; safe to delete at closeout |
| FX-029 | Disposable direct-API member for Fix Phase 2 profile-pipeline verification (TR-181's 3rd/4th bugs, TR-252) | User 73 (`VGC73`), direct-API signup | ad hoc (`FIX_PLAN.md` Phase 2 — profile pipeline) | `Fix Phase2 Profile Probe` | Confirm `GET /profile`'s raw `{status,data}` envelope shape directly (to verify the `getProfile()` unwrap fix is correctly targeted); confirm `PATCH /profile` no longer returns the password hash (TR-252) | Live `GET /profile` call confirmed the raw envelope shape exactly as expected. Live `PATCH /profile` call (with `name` changed, `dob` included to work around the separate, unfixed TR-234) confirmed the response contains no `password`/`password_reset` field | Yes, zero balance, no dependent records, no destructive value | Not yet deleted; safe to delete at closeout |

## Financial snapshots

| Snapshot ID | Workflow/case | Persona/admin wallet IDs and balances | PTS rate/components | Latest ledger/passbook IDs | Idempotency key label | Before/after reconciliation | Result |
|---|---|---|---|---|---|---|---|
| FIN-001 | WF-07 Constitutional points award (TR-190 direct-API workaround) | `VGC50` points wallet 83: 0→1000; real admin `VGC37` points wallet 47: 26,172.15→26,472.15 | Not applicable (Points, not PTS) | Wallet 83, wallet 47 | Not applicable (admin award, no idempotency key) | 1000 credited to member + 300 (30%) credited to admin = 1300 total minted, zero debits — matches the documented Constitutional-award mint model exactly | Reconciled — Pass |
| FIN-002 | WF-06 instant Points transfer | `VGC50` points wallet 83: 1000→900; `VGC51` points wallet 86: 0→100 | Not applicable | Wallet 83, wallet 86; passbook entry visible on sender side | Not applicable (transfer form has no visible idempotency-key UI; not tested this pass) | Debit 100 = credit 100 exactly, zero fee, atomic and final as documented | Reconciled — Pass |
| FIN-003 | WF-09 marketplace Buy Now | `VGC50` points wallet 83: 900→893.05 (item id 2, seller id 12/`VGC Admin` real seller, not a disposable fixture — order itself is disposable, the seller/item are real live-seeded catalog data) | Not applicable | Order id not captured (visible in `/market/orders` as "Pending Pod"); wallet 83 | Not applicable (Buy Now has no visible idempotency-key UI; not tested this pass) | Escrow hold of exactly 6.95 points, stock/seller side not independently re-verified this pass | Reconciled (buyer side) — Pass |
| FIN-004 | WF-09 marketplace cart checkout | `VGC50` points wallet 83: 893.05→883.27 (item id 14, seller id 12/`VGC Admin` real seller — same caveat as FIN-003) | Not applicable | Cart id 3 → cleared post-checkout; wallet 83 | Not applicable | Exactly 9.78 points debited, cart emptied exactly once, no double-charge | Reconciled (buyer side) — Pass |
| FIN-007 | WF-17 full Secure contract lifecycle (escrow + 5% fee) | Giver `VGC53` points wallet 92: 50→29 (−21 at appointment); Taker `VGC51` points wallet 86: 150→170 (+20 at verify-completion); real admin `VGC37` points wallet 47: 26,502.15→26,503.15 (+1, listing fee only) | Not applicable | Contract id 36, application id 25 | Not applicable | Perfect three-way reconciliation: 21 debited from giver at appointment = 20 released to taker at verify-completion + 1 retained by admin as the non-refundable listing fee. Matches the SRS-documented escrow+5%-fee model exactly | Reconciled — Pass |
| FIN-006 | WF-17 full Independent contract lifecycle | Giver `VGC53` points wallet 92: 100→50 (after a disposable +100 constitutional award to fund the test); Taker `VGC51` points wallet 86: 100→150 | Not applicable | Contract id 35, application id 24; rating recorded | Not applicable | Verify-completion returned `amount_transferred:50`; giver debited exactly 50, taker credited exactly 50, zero fee (correct for Independent type) — fully reconciled | Reconciled — Pass |
| FIN-005 | WF-09 order cancellation — TR-194 evidence | `VGC50` order id 24 (item id 2, `VGC_POINTS` 6.95) cancelled | Not applicable | Order id 24 → `Cancelled`; token wallet 82: 0→6.95 (unintended); points wallet 83 unchanged at 883.27 | Not applicable | **Not reconciled — this IS the defect**: 6.95 points spent were never refunded in points; 6.95 tokens appeared instead. `VGC50` now holds an unintended 6.95-token balance as a direct side effect of TR-194; left in place as live evidence, zero real-world/other-member impact, disposable fixture | Documents TR-194 — not a clean financial pass |

## Untracked pre-existing fixtures discovered this session

`searchTableContent` on `user` (table 839577) surfaced accounts never recorded in this ledger, sitting between the last-known `VGC48` cleanup and this session's start: `VGC44`/`VGC45`/`VGC46` ("E2E Test User"/"E2E Test User B", `vgce2e+...@example.com` alias convention, pre-2026-07-19), `VGC47` ("E2E Test Admin", already known from `TEST_REGISTER.md` TR-148), and `VGC49` ("E2E Guardian 20260719", `vgcreinventinggaming+e2e20260719g1@gmail.com`, created 2026-07-20 00:33 AEST — after this run's last logged execution entry `EX-038` at 00:02 and before this session's kickoff, so from an unlogged/uncommitted prior attempt at this same resume). All four are `email_verified_at: 0` (never completed signup) and, on inspection, distinct from real user `VGC43` ("Barkha Gorana", real personal Gmail address) which sits in the same ID range and must never be touched. None of `VGC44`/`45`/`46`/`49` were deleted — `deleteTableContentItem` explicitly requires user confirmation before a destructive call, deleting is not necessary for this run's testing (fresh `VGC50`–`53` personas were created instead, fully controlled by this session), and leaving inert unverified rows in place causes no interference. Flagging for the owner: these four rows are safe to delete whenever convenient (zero dependent wallets/records expected given `email_verified_at:0`, unconfirmed without an explicit per-row check).

## System-wide configuration snapshots

| Setting | Before value/evidence | Temporary value | Authorised window | Restored value/evidence | Status |
|---|---|---|---|---|---|
| PTS rate/theta/reserve/budgets | `pts_components` id=1 (table 845058) | Idempotent no-op test of "Update R & A" (R=0, A=0, matching displayed values) at `/admin/pts` inadvertently created the singleton row for the first time via an unwrapped, pre-audit-log `db.add_or_edit`, silently setting `theta` to the raw schema default `0` instead of the documented `0.00005` default (TR-208) — discovered via direct table read, not from the (misleadingly stale-cached) UI. Restored `theta` to `0.00005` via "Adjust θ" with an explicit reason note; `reserve_inr`/`hard_assets_inr` were already `0` before and after, so no residual drift there. | 2026-07-20 11:26–11:28 | table 845058 row id=1 | **Global platform state modified then restored same session — see TR-208/TR-209** |
| Vacation mode | Not read yet | No change authorised | — | — | Untouched |
| Cloudinary preset | `vgc_blog`: unsigned; overwrite false; no explicit format/size/dimension/folder/moderation/access/transform restrictions | No change authorised | — | — | Read-only snapshot complete; hardening defect logged |

## Synthetic file fixtures

| File ID | Type/size | Safe contents | Created | Uploaded record IDs | Cleanup |
|---|---|---|---|---|---|
| FILE-001 | PNG, valid small | Visible `E2E-20260719-01` marker | Pending | — | — |
| FILE-002 | PDF, valid small | Synthetic text and `E2E-20260719-01` marker | Pending | — | — |
| FILE-003 | TXT renamed as image | Synthetic invalid-MIME case | Pending | — | — |
| FILE-004 | Oversize boundary fixture | Repeated synthetic bytes only | Pending | — | — |

## Cleanup checkpoints

| Checkpoint | Local time | Active fixture count | Deleted | Retained with reason | Orphans/financial residue | Result |
|---|---|---:|---:|---|---|---|
| CL-001 | 2026-07-19 | 0 | 0 | None | None created | Pass |
| CL-002 | 2026-07-19 22:15 | 4 rows | 4 | None | No email token, notification, guardian approval or rate counter; exact user/wallet re-query zero | Pass |
| CL-003 | 2026-07-20 11:58 | All personas P-001–P-003, disposable game/group/blog fixtures, contract 35/36 | 0 (session ongoing) | All disposable fixtures retained pending final session close | One real global-state side effect this session (PTS `theta` zeroed by TR-208, `pts_components` table 845058) — confirmed remediated same session (`theta` restored to `0.00005` via "Adjust θ", re-verified by direct table read); no other non-fixture/real-user data was mutated — `VGC52` login for the TR-217 IDOR test was read-only (a `GET` only); real contract 26/app 16 (Visual Gorana/Varun ghavare) inspected read-only only, no resolution submitted | Pass, with one documented-and-remediated exception |
| CL-004 | 2026-07-21 | `blog_dislikes` id 15 (`blog_id:0, member_id:0`, junk row from TR-142 unauthenticated-POST re-verification) | 1 (via the same open, unauthenticated `DELETE` endpoint) | None | Re-queried `GET blog_dislikes/15` immediately after — confirmed `404`, zero residue | Pass |
| CL-005 | 2026-07-21 | `blog_dislikes` id 16 (`blog_id:60, member_id:53`) — live FE-085/086/DB-011 control test on real (non-fixture) blog post 60, "The Story of the Havoc Demon Hunter" by Varun ghavare | 0 — no un-vote/remove-vote endpoint exists on the backend by design (`dislike_POST.xs`/`like_POST.xs` are idempotent once a vote is set), so the residue cannot be cleared through normal API/UI paths | Row identified for direct-DB deletion; owner explicitly declined the offered cleanup and asked to log it as known residue instead | Real post's public dislike count is permanently off by 1 (0→1) until an owner/admin manually removes `blog_dislikes` row 16; no wallet/points side effect (vote switch, not first vote, so no `mutate_wallet` call fired) | Pass, with one documented, owner-declined-cleanup exception — tag `E2E-BLOG60-RESIDUE` |
