# E2E Fixture Ledger — Run `E2E-20260723-01`

Every disposable fixture created/reused this run. Naming: `E2E-20260723-01-<purpose>`
or a descriptive tag. Ledger every record ID before leaving a workflow.

| FX-ID | Description | Record refs | Marker/email | Purpose | Outcome | Cleanup? | Cleanup status |
|---|---|---|---|---|---|---|---|
| FX-R1-carry | Carried-over fix-phase persona (read-only reuse) | User 75 (`VGC75`), email-verified; has investment id 1, sponsorship id 1, loan id 1, expense id 1; guardian of minor VGC76 | `vgcreinventinggaming+fix3aprobe...@gmail.com` | Read-only/non-destructive regression checks | — | No (keep, backs prior evidence) | Retained |
| FX-CP09-e2edummy | Fresh disposable member, user-authorized dummy-data write-flow testing (2026-07-24, per explicit "it is okay to test with dummy data... we will clean up") | User 81 (`VGC81`); created across the session: 6 declarations (ids 29-34), 3 investments (ids 2/3/4 — id 3 was a silent duplicate from a since-fixed redirect bug, only discovered via the DB-level cleanup audit, not from session memory) + 9 payout rows, 2 sponsorships (ids 2/3, incl. `upi_id: abcd123@upi.in`), 1 loan request (id 2, `upi_id: abcd123@upi.in`), 1 blog draft (id 74), 1 group (id 16) + 1 membership row, 1 contract application (id 26, on contract 31), 1 cart (id 4) + 1 cart item (id 5), 1 marketplace proposal (id 1, post-TR-267 fix), 2 triggered notifications (loan-submitted → member 37, contract-applied → member 45), 3 wallets (all ₹0/0, never funded) | `vgcreinventinggaming+e2edummy1784900626@gmail.com` / `E2eDummy!2026` | Exercised the real create-side success paths for financial/loan/marketplace/group/blog/contract flows that a 0-balance/no-admin-session constraint had left only boundary-tested | Surfaced/fixed TR-263 through TR-269 (7 defects, all Resolved) | Yes | **Cleaned up 2026-07-25** — all 35 rows deleted across 13 tables + the member itself (bulk-deleted in dependency order: payouts → investments/sponsorships/loans → declarations → contract application/cart item/blog/group-membership/proposal/notifications → cart/group → wallets → user). Verified via a full re-query sweep: zero residue in any table. Spot-checked shared fixtures untouched: contract 31, market item 11, and VGC75's own investment-1/payout-1 all intact. |
| FX-FIN-a11y-education | Multiple small, immediately-cleaned-up fixtures created while finishing the remaining test-plan items (2026-07-25): a temporary 2-member group (id 17, using VGC75 + a fresh member id 82) to test TR-249's Escape/focus-restore on the Remove Member dialog; a disposable draft blog (id 75) with a real HTML embed to confirm G9's iframe sandbox attribute live; a fresh member (id 83) to confirm G4's cross-account cache clears on logout+re-login; a real course (id 1) + session (id 1) + auto-created marketplace proposal (id 2) created by VGC75 via the genuine member-facing `courses_POST.xs` to exercise the previously-untested education routes (TR-271), plus one directly-inserted enrollment row (id 1, member 45 "Checked-in") to prove the fixed attendance endpoint's attendee list populates and its Verify button works | n/a (used existing VGC75 + throwaway signups, no dedicated ledger email needed beyond what's below) | Members: `vgcreinventinggaming+a11ydummy...@gmail.com` (id 82), `vgcreinventinggaming+g4test...@gmail.com` (id 83) | Verify TR-249 (focus-trap fix), G4/G9 (security-gate items previously marked "pending browser"), and TR-271 (education fix) all live | All confirmed working; TR-249/G4/G9 pass, TR-271 fix verified end-to-end | Yes | **Cleaned up 2026-07-25, same session** — group 17 + membership, member 82 + wallets, blog 75, member 83 + wallets, course 1, session 1, marketplace proposal 2, enrollment 1 all deleted immediately after each check; verified via re-query (zero residue for author_member_id=75 blogs, zero rows in courses table, members 82/83 both gone) |
| FX-ADMIN-2FA | Real admin session, acquired 2026-07-25 to test admin-approval success paths per user direction: reused the pre-existing "E2E Test Admin" fixture (user 53, `VGC53`) via a genuine password-reset (its email is a real alias of the workspace owner's Gmail, so the reset/OTP emails are actually deliverable and were read via the Gmail tools available this session) — no admin-role promotion, no backend config change. Also created one disposable member (id 84) + one loan request (id 3, ₹3,000) purely to have a safe, genuinely-completable loan to reject as the real admin. | Admin: user 53 (`VGC53`, password now known — see below); its `admin_totp` row now has a fresh `last_login_at` (normal, expected state for a real login, not cleaned up) and real `admin_login_events`/`admin_mfa_challenges` audit rows (kept — legitimate security audit trail, not test noise). Disposable: member 84 + 3 wallets + loan 3 + 1 notification (all deleted). Also touched two **pre-existing generic dev-seed fixtures** by rejecting them for real (the point of the test, not incidental damage): declaration id 12 ("Test User 2", now `rejected`) — left in this state, since a successful rejection *is* the confirmed-correct outcome. | Admin: `vgcreinventinggaming+e2e0719admin@gmail.com` / `AdminE2eTest!2026` (new known password, was previously unknown/inaccessible) | Prove `enforce_role`'s 2FA gate (TR-176) and every admin approval/completion endpoint's validation boundary work correctly with genuine admin credentials, without ever completing the final wallet-mutating branch (loan disbursement / surrender debit / declaration credit) | Confirmed TR-176 end-to-end; found/fixed TR-272 (admin panel React key + surrender button/badge bug) | Partial (by design) | **Disposable parts cleaned up 2026-07-25** (member 84, its wallets, loan 3, the loan-rejected/loan-submitted notifications — verified via re-query, zero residue). **The admin account itself (VGC53) and its now-real password/fresh 2FA session are intentionally retained** — it's a pre-existing test fixture, not something created this pass, and having a known-working admin login is valuable for any future session that needs one. Declaration 12's `rejected` status was the deliberate test outcome, not reverted. |
| FX-COURSE-DISPUTE | Fresh disposable teacher (id 90) + 2 students (ids 91/92), 2026-07-25, to build and verify the new course dispute mechanism (TR-278) per explicit user direction. One course (id 6) with one online session; both students purchased, checked in (auto-Verified), each raised a dispute on their own order — student A (91) via the real browser UI (`/market/orders/{id}` → Raise a dispute), student B (92) via direct API call for speed. | Teacher 90 + 3 wallets, students 91/92 + 3 wallets each; 1 course, 1 proposal, 1 session, 1 marketplace item, 2 orders, 2 enrollments, 2 marketplace_order_disputes | `vgcreinventinggaming+disputeteacher1784938376@gmail.com` / `+disputestudenta.../+disputestudentb...@gmail.com` | Verify the new course-dispute mechanism end-to-end: raising a dispute (both via UI and API), both admin resolution paths (full_refund and vendor_favour), the payout-blocked-while-disputed gate, and correct final payout math with no double-payment | **Found and fixed TR-279** (admin resolve-dispute's full_refund/vendor_favour always 400'd, a pre-existing gateway validation bug, discovered because this was the first live exercise of those two paths). After the fix: full_refund correctly restored student A's full balance; vendor_favour correctly dismissed student B's dispute with **zero wallet credit at that point** (verified via direct balance check — the money only ever moved once, later, through the course's own aggregate payout); payout-request correctly blocked while either dispute was open, then correctly computed 36/4 tokens (90/10 of the single non-refunded 40-token order) once both were resolved. | Yes | **Cleaned up 2026-07-25, same session** — all 3 members + 9 wallets, course/proposal/session/item, both orders, both enrollments, both disputes all deleted; verified via re-query (zero residue across users/wallets/courses/sessions/orders/enrollments/marketplace_order_disputes tables). |
| FX-COURSE-CONSUMPTION | Fresh disposable teacher (id 88) + student (id 89), 2026-07-25, for a full course-consumption feature test per explicit user request. Teacher created 3 disposable courses (ids 3/4/5) across the test: course 3 (in-person, ₹100/session) ran the full happy-path lifecycle (purchase → checkin via QR → teacher verify → session end → both rating directions → teacher payout request → admin payout, 90/10 split); course 4 (online) verified the auto-checkin-to-Verified path and was the one used to discover and reproduce both TR-276 (fatal purchase bug) and TR-277 (cancel-refund-after-consumption exploit) live; course 5 (online) was a clean isolated re-verification that TR-277's fix correctly blocks post-consumption cancel while not regressing pre-consumption cancel. Student's token wallet was directly funded (200 tokens, admin table edit) rather than routed through a declaration, since the test only needed spending power, not to re-test the declaration-credit path already covered by FX-WALLET-MUTATION. | Teacher 88 + 3 wallets, student 89 + 3 wallets; 3 courses, 3 marketplace_proposals, 3 sessions, 3 marketplace_items, 6 orders (2 legitimately cancelled, 1 exploited pre-fix then reverted, others part of normal flow), 4 enrollments, 2 session_ratings | `vgcreinventinggaming+eduteacher1784935951@gmail.com` / `vgcreinventinggaming+edustudent1784935951@gmail.com` | Verify the entire course lifecycle (purchase → attendance → rating → payout) works correctly end-to-end for the first time, and specifically test whether any dispute/report mechanism exists for a purchased course | **Found and fixed TR-276 (Critical)**: the first-ever course purchase attempt fataled after debiting the buyer (a XanoScript syntax bug in the auto-enrollment code) — reverted the one pre-fix broken order/debit/stock-decrement by hand, then re-verified the complete fixed flow with exact wallet/points math. **Found and fixed TR-277 (Critical)**: a buyer could self-cancel/refund a course purchase in full even after being fully Verified for every session, live-reproduced and then fixed with a consumption-aware precondition, verified against 3 scenarios (regression check, exploit blocked, edge case). **Confirmed TR-278**: the generic marketplace dispute endpoint is permanently unreachable for course orders (they never reach the required `pod_submitted` status) — flagged as a real feature gap, not fixed without a scope decision. | Yes | **Cleaned up 2026-07-25, same session** — both members + all 6 wallets, all 3 courses/proposals/sessions/marketplace_items, all 6 orders, all 4 enrollments, both ratings all deleted; verified via re-query (zero residue for member_id 88/89 across users/wallets/courses/sessions/orders/enrollments/session_ratings tables). |
| FX-TR228-TR274 | Two fresh disposable members created 2026-07-25 to verify the TR-228 (blog archive) and TR-274/TR-275 (course-creation + admin proposal review) fixes end-to-end. Member 86 (`vgcreinventinggaming+blogtest1785010001@gmail.com`): created blog 76, published it, archived it (the exact repro path for TR-228). Member 87 (`vgcreinventinggaming+courseui1785012001@gmail.com`): created course 2 + session 2 through the new `ProposeCourseScreen` UI, which auto-created marketplace proposal 3; then, using a real admin session (VGC53, reused via its still-valid 2FA window from `FX-ADMIN-2FA` — no fresh 2FA login performed), listed the proposal via the new "List Course" admin action, which created marketplace item 26 and transitioned course→`listed`/session→`Scheduled`. | Member 86 + 3 wallets + blog 76; member 87 + 3 wallets + course 2 + session 2 + marketplace item 26 + proposal 3 | `vgcreinventinggaming+blogtest1785010001@gmail.com` / `vgcreinventinggaming+courseui1785012001@gmail.com` (both auto-generated passwords, not reused) | Prove TR-228's archive fix and TR-274/TR-275's full member-propose → admin-list chain work correctly with real data, not just API-boundary checks | All three fixes confirmed working end-to-end via real browser UI interaction (Playwright) plus direct API verification of final DB state | Yes | **Cleaned up 2026-07-25, same session** — blog 76, member 86 + wallets, session 2, course 2, marketplace item 26, proposal 3, member 87 + wallets all deleted; verified via re-query (zero residue for both members across users/wallets/blogs/courses/sessions/marketplace_items/marketplace_proposals tables). |
| FX-WALLET-MUTATION | Fresh disposable member (id 85), created 2026-07-25 per explicit user direction ("I need to test final wallet mutations as well... Go ahead and do end to test") to complete the three real wallet-mutating admin actions this session had held off on: declaration verify (credit), token-surrender complete (debit), and loan approve (credit+debit) — all with real wallet-balance checks before/after to mathematically confirm correctness. | User 85; created and then fully processed: declaration id 35 (Donation ₹1,250, verified → INR wallet credited exactly ₹1,250), declaration id 36 (Token Purchase ₹500, verified → token wallet credited exactly 50 = 500/10), token surrender id 22 (20 tokens, completed → token wallet debited exactly 20, 50→30), loan id 4 (₹250 requested, approved/disbursed → INR wallet +250 to 1,500, token wallet −25 to 5, the Year-1 help-rate debit); 3 wallets; 2 notifications (loan_submitted, loan_approved) | `vgcreinventinggaming+wallettest1784931991@gmail.com` / `WalletTest!2026` | Prove the real wallet math is correct on all three admin-completion flows, using genuine before/after balance checks (not just precondition/validation-boundary checks) | **Found and fixed TR-273 (Critical)**: declaration verification had been crediting ₹0 on every historical verification (read a field, `amount_paid`, that no code path in the entire codebase ever sets) — caught immediately because this was the first time this session actually checked a post-verification wallet balance rather than stopping at the precondition. After the fix, all three flows' math confirmed exactly correct: declaration credit, surrender debit, loan disbursement credit+debit all landed on the exact expected balances. | Yes | **Cleaned up 2026-07-25, same session** — member 85, both declarations, the surrender, the loan, all 3 wallets, and both notifications all deleted; verified via a full re-query sweep (zero residue for member_id/id 85 across users/loans/token_surrenders/declarations tables). |
