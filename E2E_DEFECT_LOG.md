# VGC E2E Defect Log

**Run:** `E2E-20260719-01`
**Started:** 2026-07-19 (Australia/Melbourne)
**Purpose:** sanitized defects confirmed during the exhaustive E2E run. This file is the run-scoped source of truth until each row is reconciled into `TEST_REGISTER.md`.

**Reconciled 2026-07-21:** every row in this file (TR-163–TR-227), including the 4 accessibility/UX observations now tracked as TR-218–TR-221, has been merged into `TEST_REGISTER.md`'s Active Issues table. This file remains the detailed evidence record; `TEST_REGISTER.md` is now the authoritative status tracker going forward.

No passwords, bearer tokens, OTPs, API secrets or reset links belong in this file.

| ID | Severity | Layer | Area | Confirmed result | Evidence | Status |
|---|---|---|---|---|---|---|
| TR-227 | High | Backend | Wallets — single-wallet endpoint can never find live wallets because API enum values are uppercase but stored wallet `type` values are lowercase | `GET /api:wallets/me/{currency}` declares enum values `INR`, `VGC_TOKEN`, `VGC_POINTS`, then queries `wallets.type == $input.currency`. Live wallet rows store `type` as lowercase (`inr`, `token`, `points`), and the frontend wrapper `getWallet(currency)` passes the uppercase `Currency` enum. Confirmed with a fresh disposable authenticated member (`vgcreinventinggaming+e2e0721direct1784596060@gmail.com`): `GET /me/VGC_POINTS` returned `404 Wallet not found` even though signup created the member's three wallets and `GET /wallets/me` works elsewhere. This is the single-wallet variant of the same wallet-type/casing family already seen in TR-181/TR-191, but it affects a separate endpoint and any future screen that uses `getWallet()`. | Direct authenticated API reproduction 2026-07-21 against disposable member; `XANO/api/wallets/me/currency_GET.xs` query compares uppercase enum to lowercase stored type | Open |
| TR-226 | High | Backend / Infrastructure | Xano file upload endpoint is wired in the frontend but cannot work on the current Xano instance | `FrontEnd/src/api/system.ts` exposes `uploadFile()` against `POST /api:KVaxK9ev/files/upload`; the backend then calls `storage.create_attachment`. A live authenticated multipart upload with a tiny 1x1 PNG and allowed purpose `blog_attachment` returned `403 {"message":"Not supported. Please upgrade your Xano instance."}` before any file metadata row could be created. This means every feature relying on the Xano file path (`declaration_proof`, `pod`, `kyc`, `avatar`, `event_submission`, `group_post`, `cv`, `icon`) is non-functional on the current infrastructure unless it uses the separate Cloudinary direct-upload path. No file row was created, so there was no cleanup residue. | Direct authenticated API reproduction 2026-07-21 against disposable member; `files/upload_POST.xs` uses `storage.create_attachment`; response was Xano plan/capability 403 | Open |
| TR-225 | Medium | Frontend | Edit Blog screen renders completely blank on any fetch failure — no error state, no retry, no explanation | `EditBlogScreen` (`WriteBlogScreen.tsx` lines 328-329) only handles the loading case (`if (blog.isLoading) return <SkeletonList .../>`) and then `if (!blog.data) return null;` — there is no `isError`/`ErrorState` branch at all, unlike every sibling detail screen this session (Donor/Investment/Sponsorship/Season/Course all show a clean "X not found" + Retry via `ErrorState`). Confirmed live: navigated to `/blog/47/edit` as `VGC52` — the real blog id 47 exists (`in_review` status, confirmed via a separate admin-token fetch) but correctly returns `403`/`404` to a non-author, non-admin viewer (that access restriction itself is correct — an unpublished post shouldn't be editable/visible to unrelated members). However the screen response to that failure is a **completely blank page** — no title, no message, no retry button, nothing but the bottom nav — leaving the user with zero information about whether the page is broken, still loading, or intentionally inaccessible. | Live reproduction 2026-07-21 at `/blog/47/edit` as `VGC52` (non-author, non-admin): blank content area, one console `404` on `GET /blog/47`; separately confirmed blog 47 is real and `in_review` via an admin-token fetch; `WriteBlogScreen.tsx` lines 328-329 (`EditBlogScreen`) confirms no `isError` handling | Open |
| TR-224 | High | Both | Gaming Event Detail screen calls an endpoint that was never built on the backend | `EventDetailScreen.tsx` (line 36) calls `GET /events/{id}` on the `gaming-seasons` canonical group ("reuse gaming-seasons canonical for events", per its own code comment) — but no such route exists anywhere in `XANO/api/gaming_seasons/`; the only `events/{id}/*` endpoints that exist are `submissions_GET`, `submissions_POST`, and `results_POST` (sub-resources), never a bare event-detail `GET`. Confirmed live: navigating to `/gaming/events/1` (and via direct `curl GET events/1` on the `gaming-seasons` group, no ID substitution ambiguity) both return `404 {"code":"ERROR_CODE_NOT_FOUND","message":"Unable to locate request."}` — Xano's own generic gateway-level "no matching route" response, not a custom app-level "not found" message, confirming the route is structurally absent rather than merely lacking data. The raw, untranslated `"Unable to locate request."` string is also shown directly to the user (a §9.6/§18 "no raw backend errors" violation, same pattern flagged in TR-187's secondary finding), instead of a friendly "Event not found" matching every sibling detail screen (Season/Election/Course/Investment/Donor/Sponsorship all show clean custom messages for the identical scenario). **Independent of and compounding TR-199**: even once Pioneer Candidacy is fixed and seasons/events begin being created, `/gaming/events/:id` would still be permanently broken for every event, because the endpoint it depends on doesn't exist at all — this isn't a data problem, it's a missing route. | Live reproduction 2026-07-21 at `/gaming/events/1` as `VGC52` (local dev): raw `"Unable to locate request."` shown to the user; direct `curl GET events/1` on the `gaming-seasons` canonical group confirms `404 ERROR_CODE_NOT_FOUND` (generic gateway response); `EventDetailScreen.tsx` line 36 vs. `find XANO/api/gaming_seasons -ipath "*events/id*"` (no bare detail GET exists) | Open |
| TR-223 | Critical | Both | Sponsorship creation has never worked — 7th confirmed instance of the identical `amount`-vs-`amount_inr` field-name mismatch | `CreateSponsorshipScreen.tsx` (lines 16-27) sends `{amount: parseFloat(amount), conditions, upi_id: upiId}`, but `financial_sponsors/sponsorships_POST.xs` requires `decimal amount_inr` (not `amount`). Confirmed live: filled Amount (₹100), Conditions, and UPI ID completely and clicked "Submit Sponsorship" as `E2E Adult C` (`VGC52`) at `/financial/sponsorships/new` — `400 "Missing param: amount_inr"`. **No sponsorship has ever been created through the product UI.** Seventh confirmed instance this session of the identical `amount`→`amount_inr` naming drift (declarations TR-149 historically, investments TR-200, loans TR-201, expenses TR-202, and now sponsorships) — this is clearly one shared copy-paste origin propagated across every financial creation form in the codebase, not five/six/seven independent mistakes. Unlike the others, the field labels here are otherwise correctly matched to the backend (`conditions`, `upi_id` both correct) — only the amount field's name is wrong, so this would be a trivial one-line fix once identified, same as its siblings. | Live reproduction 2026-07-21 at `/financial/sponsorships/new` as `VGC52` (local dev): `400 "Missing param: amount_inr"`; `sponsorships_POST.xs` vs `CreateSponsorshipScreen.tsx` source | Open |
| TR-222 | Medium | Frontend | Groups — "New Post" composer reachable by non-members, fails only at submission | `CreatePostScreen.tsx` (`/groups/{id}/post/new`) renders the full compose UI (Text/Photo/Poll/Link/GIF/Video/File type selector, content field, Post button) regardless of whether the viewer is a member of the target group — confirmed live as `E2E Adult C` (`VGC52`, not a member of group 11) navigating directly to `/groups/11/post/new`: the form loads fully editable, with no "join to post" prompt or membership check anywhere in the UI. The backend correctly blocks the actual submission (`posts_POST.xs` line 34, `precondition ($membership != null)`) — confirmed live via direct API call with a real non-member token: `403 "You must be an active member of this group to post."` — so this is not a security gap, purely a UX one: a non-member can spend time composing a post (including uploading media) only to have it rejected at the final step with a somewhat technical error message, instead of being redirected to the group page or shown a "Join to post" prompt before they start. | Live reproduction 2026-07-21 at `/groups/11/post/new` as `VGC52` (non-member); direct `POST groups/11/posts` with a real non-member token confirms `403` server-side | Open |
| TR-163 | High | Backend | Password reset | Unknown email returns `No user found for that email.` while a known email returns success, allowing account enumeration. | RUN-007–008; API-096 / FE-069 | Open |
| TR-164 | Critical | Both | Registration / safeguarding | UI accepts a future DOB as a negative-age minor. More importantly, direct public `POST /signup` accepted a 14-year-old DOB, issued a valid auth token and created a normal member plus three wallets without guardian approval. Fixture and wallets were fully deleted. | RUN-012, RUN-015–016; CL-002 | Open |
| TR-165 | High | Backend | CORS | Xano reflects an arbitrary hostile Origin while enabling credentials and advertising write methods/headers. Bearer-only authentication reduces some cookie-CSRF exploitability but the origin policy is still unsafe. | EX-015; read-only OPTIONS/GET probes | Open |
| TR-166 | High | Backend | Backup admin | Anonymous backup-status response exposes operational security state including backup-admin identity, primary-login timing and vacation/activation state. | EX-014; anonymous read-only probe | Open |
| TR-167 | High | Backend | HTTP method safety | Multiple GET handlers can perform lifecycle, financial or notification mutations. Confirmed source families include reset request, guardian expiry, education/session lifecycle, elections, seasons, groups, loans, orders, transfers and wallet creation. | Static endpoint review; no valid production IDs used | Open |
| TR-168 | Medium | Frontend | Production headers | Production frontend HTML/JS lacks CSP, frame protection, MIME-sniff protection, Referrer Policy and Permissions Policy. HSTS is present. | EX-015 | Open |
| TR-169 | Medium | Frontend | Routing / operations | Unknown paths and `/robots.txt` return the SPA shell with HTTP 200, producing soft-404 and crawler/monitoring ambiguity. | EX-013 | Open |
| TR-170 | Medium | Frontend | Public legal/guardian routes | Anonymous Terms and Privacy placeholders render authenticated-style Home/Wallet/Explore/Community/Profile navigation. Protected links redirect guests and discard signup context. | RUN-013–014 | Open |
| TR-171 | Medium | Backend | API contract integrity | Parameters documented and sourced as optional remain gateway-required. Confirmed on `/contracts` filters and backup-admin `requester_member_id`. **Expanded 2026-07-21 during the systematic endpoint sweep**: `expenses/expenses/me_GET.xs` declares `int? page?=0 filters=min:1` and `int? per_page?=0 filters=min:1|max:100` — both optional with a default of `0`, but the default value itself fails each field's own `min:1` filter, so the endpoint 400s with `"Missing param: page"` (then `"Missing param: per_page"`) unless BOTH are explicitly supplied with a value ≥1, contradicting their own `?` (optional) declaration. Confirmed live via direct `curl`: bare `GET /expenses/me` (no query params) → `400`; supplying only `page=1` → `400` on `per_page`; supplying both → correctly reaches the next check (`401` with no token). **Not currently user-facing**: `FrontEnd/src/api/expenses.ts`'s `getMyExpenses()` always hardcodes `{page:1, per_page:50, ...filters}`, so the live product never triggers this path — but any other caller (a future FE change, a direct API integration, or a mobile client built independently against the documented "optional" contract) would break on the very first call. Grepped all 295 endpoints for the identical `?=0 filters=min:1` pattern — confirmed isolated to this one file, not systemic. | EX-014; live `curl` reproduction 2026-07-21: `GET expenses/me` (bare) `400`, `+page=1` `400`, `+page=1&per_page=1` correctly reaches `401`; `expenses/me_GET.xs` lines 7-15 vs. `expenses.ts` lines 42-50 (FE always supplies both, masking the defect) | Open; expand via endpoint sweep |
| TR-172 | High | Both | Cloudinary uploads | `vgc_blog` is unsigned with no explicit format, byte/dimension, folder, moderation, access-control or transformation restrictions. App callers validate only via `accept="image/*"`, retain only URLs and have no ownership/deletion path, creating abuse, performance and orphan-asset risk. | EX-020; source caller inventory | Open |
| TR-173 | High | DevOps | Credential hygiene | Legacy local Xano Git history contains a Metadata API token. The current `.env` is ignored and was tightened from mode `0666` to `0600`; the new private `VGC-Reinventing/xano` remote was seeded from secret-scanned, history-free commit `8310fc0`, so the legacy credential is not in that remote. No token value was reproduced. | Local Git/filesystem audit; EX-032–033 | Partially remediated; token rotation parked by user as low priority |
| TR-174 | Medium | Backend | Auth — Email verification token invalidation race | `resend-verification_POST.xs` invalidates existing unused `email_verification_tokens` rows with a read-then-write pattern (`db.query ...used==false` then a `foreach` of individual `db.edit ...used:true`) with no `db.transaction`/atomic guard. Two `resend-verification` calls fired concurrently for the same email each read the "old tokens" set before either write lands, so both newly-created tokens remain simultaneously valid — either one independently completes `verify-email`, not just the latest. Confirmed directly against production Xano (`x8ki-letl-twmt.n7.xano.io`) with two parallel `curl` POSTs (no browser involved): both returned `200 {"message":"Verification email sent"}`, both resulting tokens each verified successfully (`200 {"verified":true}`), and only a third replay of an already-used token correctly failed `400`. Weakens the intended "requesting a new code invalidates the old one" hygiene property — an old/leaked code stays live until someone actually uses one of the concurrently-issued tokens. Also observed as a **frontend-only, non-production** artifact: `VerifyEmailScreen.tsx`'s auto-send-on-mount effect uses `const autoSentRef = useState(false)` as a mount guard instead of `useRef`, so under React 18 `StrictMode` (dev server only — confirmed absent on the Vercel production build via network-tab request counts) the guard fails and two `resend-verification` calls fire per page load, reliably reproducing the race with zero deliberate concurrency. Recommend wrapping the invalidate+create sequence in `db.transaction` (or an atomic `where used==false` conditional update) and separately switching the FE guard to `useRef`. | Direct concurrent-`curl` reproduction against production Xano (2026-07-20, disposable `E2E-20260719-01` fixture `vgcreinventinggaming+e2e0719c@gmail.com`); dev-vs-prod network/mailbox comparison for the FE trigger | Open |

| TR-203 | High | Frontend | Admin Members search always returns zero results — wrong envelope key, fails silently instead of crashing | `adminListMembers()` (`FrontEnd/src/api/admin.ts:75-81`) defensively unwraps the response with `Array.isArray(res) ? res : (res as Paged<AdminMember>).items ?? []` — but the live `GET /admin/members` endpoint returns `{"members":[...], "count":N}`, not `{"items":[...]}"`. Since the response is a non-array object, the code falls through to `.items` (undefined) `?? []`, silently returning an empty array every time regardless of how many real matches exist. Confirmed live as 2FA-verified `VGC53`: searched "E2E Adult A" — the raw API response correctly returned one full matching member record (including, incidentally, the raw password hash — another instance of the TR-177 pattern on a different endpoint), but the UI rendered nothing, no error, no "no results" message distinguishable from a genuine empty state. **Admin cannot find or manage any specific member through the search box** — this silently defeats a core admin governance capability (member lookup/suspend/impersonate/erasure processing all start from this search). Unlike TR-184/195/196 (which crash the whole app), this fails silently, which is arguably worse for an admin who might reasonably conclude "no such member" and give up. | Live reproduction 2026-07-20 at `/admin/members` as 2FA-verified `VGC53` (local dev); raw `GET /admin/members?search=...` response vs. `adminListMembers()`/`AdminMembersScreen.tsx` source | Open |
| TR-202 | Critical | Both | Expense logging has never worked — same `amount`-vs-`amount_inr` field-name mismatch as declarations/loans, plus a likely second mismatch on multi-word payment modes | `expenses_POST.xs` requires `decimal amount_inr`; `FrontEnd/src/api/expenses.ts:6-22`'s `logExpense()` sends `amount`. Confirmed live: filled every field on `/expenses/new` (₹100, Cash, Miscellaneous → Unclassified Expense, remark) as `E2E Adult A` and submitted — `400 "Missing param: amount_inr"`. Separately, by source inspection only (not yet live-reproduced): `payment_mode` enum values are `Title_Case_With_Underscores` (`"Debit_Card"`, `"Net_Banking"`, `"Mobile_Wallet"`, `"Cheque"`, `"EMI"`, `"BNPL"`, `"Other"`, plus plain `"Cash"`/`"UPI"`), but the dropdown's visible option labels use spaces ("Debit Card", "Net Banking", "Mobile Wallet") — if the frontend submits the label text verbatim, every multi-word payment mode would independently fail even once `amount_inr` is fixed; only `Cash`/`UPI`/`Cheque`/`EMI`/`BNPL`/`Other` happen to match by coincidence. **No expense has ever been logged through the product UI with a non-`amount_inr`-blocked field combination.** Sixth confirmed instance this session of the identical `amount`→`amount_inr` naming mismatch (declarations, token surrender historically per TR-149's pattern, loans, expenses) — strongly suggests this specific convention drift is copy-pasted across multiple financial forms from one shared original mistake. | Live reproduction 2026-07-20 at `/expenses/new` as `VGC50` (local dev); `expenses_POST.xs` vs `expenses.ts` source; payment-mode casing risk identified by source only, not live-confirmed | Open |
| TR-201 | Critical | Both | Loan request has never worked — wrong amount field name, and a required `upi_id` field that doesn't exist anywhere on the form | `loans/loans/request_POST.xs` requires `decimal amount_inr`, `text upi_id` (required, no `?`) and `text purpose`. `FrontEnd/src/api/loans.ts:6-11`'s `requestLoan()` sends `{amount, purpose, repayment_months?}` — wrong field name (`amount` instead of `amount_inr`), and `upi_id` is never collected or sent at all; `LoansScreen.tsx`'s Request Loan tab has no UPI-ID input control anywhere. Confirmed live: filled Amount/Purpose/Repayment Period completely and clicked "Request Loan" as `E2E Adult A` — `400 "Missing param: amount_inr"`. Even if that were fixed, the missing `upi_id` would block every submission identically to TR-200's `start_date` gap. **No loan has ever been requested through the product UI.** Same recurring pattern this session: a "create X" flow with financial implications, broken via field-contract mismatch on first live attempt. | Live reproduction 2026-07-20 at `/loans` (Request Loan tab) as `VGC50` (local dev); `request_POST.xs` vs `loans.ts`/`LoansScreen.tsx` source | Open |
| TR-200 | Critical | Both | Investment creation has never worked — three independent field-contract mismatches between `CreateInvestmentScreen`/`createInvestment()` and the live endpoint | `financial_investments/investments_POST.xs` requires `enum option {values:["A_10pct_lumpsum","B_8pct_quarterly"]}`, `decimal principal_inr` and `timestamp start_date` (no `?`, required). `FrontEnd/src/api/financial.ts:24-34`'s `createInvestment()` is typed `{option: 'A' \| 'B'; amount: number; investor_email?: string}` and sends exactly that shape — wrong enum values (`'A'`/`'B'` instead of the full `A_10pct_lumpsum`/`B_8pct_quarterly` strings), wrong field name (`amount` instead of `principal_inr`), and `start_date` never sent at all despite being required. Confirmed live: selected "Option A", entered ₹1000, submitted as `E2E Adult A` — `400 "Input 'A' is not one of the allowable values."` (the option mismatch surfaces first; the other two mismatches would each independently block submission even if this one were fixed). **No member has ever been able to create an investment through the product UI.** | Live reproduction 2026-07-20 at `/financial/investments/new` as `VGC50` (local dev); `investments_POST.xs` vs `CreateInvestmentScreen.tsx`/`financial.ts` source | Open |
| TR-199 | Critical | Frontend | Pioneer Candidacy registration is completely unusable — the form has no field for a required parameter, so no member can ever submit | `PioneerCandidacyScreen.tsx` has zero references to `game_id`/`gameId`/"Game" anywhere in its source (confirmed by full-file grep) — the form collects Proposed Season Name, Season Preface/Vision, VGC Points Budget and Funding Type only. Confirmed live: filled every visible required field and clicked "Submit Candidacy" as the disposable admin persona — `400 "Missing param: game_id"`. Since there is no control anywhere on this screen to select or supply a game, and `Community Games` on the same tab shows "No games listed yet" (no games exist to select from even if a control existed), **Pioneer Candidacy can never be successfully registered through the product UI, for anyone, in the current state** — this is a core §11 Gaming feature that is completely dead on arrival. Related to but distinct from the general §18 "Gaming lifecycle may be impossible" hypothesis — this is a concrete, confirmed instance of a specific missing required field. | Live reproduction 2026-07-20 at `/gaming/pioneer-candidacy` as 2FA-verified `VGC53` (local dev); `PioneerCandidacyScreen.tsx` source confirms no `game_id` field exists anywhere | Open |
| TR-198 | Medium | Both | Admin Blog Review list always shows "Member #?" — wrong field name, author is never identifiable from the list | `AdminBlogScreen.tsx:62` reads `blog.author_name ?? \`Member #${blog.author_id ?? '?'}\``, but the live `GET /admin/blog` response has neither field — the real field is `author_member_id` (confirmed live: blog id 47 "Test Blob" returns `author_member_id:38`, no `author_name`/`author_id` anywhere). So every row in the admin review queue permanently shows the literal placeholder "Member #?" regardless of who actually wrote it, making it impossible for an admin to identify an author from the list view without opening each blog individually (not yet checked whether the detail/review screen has the same bug). Confirmed live via the real admin panel (2FA-verified `VGC53`) showing exactly this on the one seeded `in_review` blog. | Live `/admin/blog` inspection 2026-07-20 as 2FA-verified `VGC53`; `GET /admin/blog` raw response vs. `AdminBlogScreen.tsx` source | Open |
| TR-197 | Critical | Frontend | Blog rich-editor "HTML embed" is a full stored-XSS-to-session-hijack vector — `sandbox="allow-scripts allow-same-origin"` defeats the sandbox entirely | Confirms the §18 "Risky embedded HTML sandbox/CSP" hypothesis with concrete, safe (non-exfiltrating) live proof. The `/blog/new` rich editor's "Insert HTML embed (renders raw HTML with live preview)" button renders author-supplied raw HTML in an `<iframe srcdoc="...">` with `sandbox="allow-scripts allow-same-origin"` (confirmed live via `iframe.getAttribute('sandbox')`). This combination is a well-known anti-pattern: `allow-same-origin` on a `srcdoc` iframe grants it the embedding document's own origin instead of the usual opaque/`null` sandboxed origin, so `allow-scripts` scripts inside it run with **full same-origin access to the parent page** — including its `localStorage`. Confirmed live (inert, non-exfiltrating check only — verified reachability, never read/printed/sent the actual value): entered `<div id="e2e-marker">E2E-20260719-01</div><script>window.__e2eHtmlEmbedExec = true;</script>` into the embed dialog; the marker script did **not** leak to the top-level `window` (`window.__e2eHtmlEmbedExec` stayed `false` there, so no full sandbox escape to the parent JS context) — but `iframe.contentWindow.localStorage.getItem('vgc.authToken') !== undefined` returned `true` with **no cross-origin `SecurityError`**, proving any script placed in this block can directly read the viewer's `vgc.authToken` (the bearer token used for every authenticated API call) and could trivially exfiltrate it via `fetch`/an image beacon to an attacker-controlled host. This fires the moment the block renders — during the **author's own live preview while writing**, and again for **every subsequent viewer**, including, per §18's existing note that `AdminBlogReviewScreen` also uses `dangerouslySetInnerHTML`/the same rendering path for moderation review — **the admin reviewing the post before publication**, making the review gate itself an attack vector against the highest-value session on the platform, not a safety net. No draft was saved/submitted — this reproduction was entirely client-side via the live preview, so nothing persisted to the backend. Recommend dropping `allow-same-origin` from the sandbox attribute (breaks same-origin JS access while still allowing visual rendering) and/or moving to a strict CSP + server-side HTML sanitisation (matching TR-168's missing-CSP finding) before this editor is used again. **Confirmed wider blast radius 2026-07-20**: `/contracts/new`'s "Contract Details & Requirements" field uses the exact same rich-editor component (same toolbar, same "Insert HTML embed (renders raw HTML with live preview)" button) — this is a shared reusable component, so the identical vulnerability applies to contract descriptions too, matching §18's existing note that contract detail/admin dispute screens also use `dangerouslySetInnerHTML`. Not re-tested live here (the blog reproduction already conclusively proves the component-level defect); fixing the shared editor component fixes both surfaces at once. | Live client-side reproduction 2026-07-20 in `/blog/new`'s HTML-embed live preview as `VGC50` (local dev); `iframe` `sandbox`/`srcdoc` attributes and cross-frame `localStorage` reachability checked directly via `browser_evaluate`, no value read or exfiltrated | Open |
| TR-196 | Critical | Frontend | Systemic: at least four more screens crash the entire app on load with the same envelope-vs-array bug as TR-184/TR-195 | Same root cause pattern, different screens — each API wrapper types its list response as a bare array (`T[]`) while the live Xano endpoint returns a paginated envelope, and each screen calls `.data.map(...)` directly on the untyped envelope object. Confirmed live, one after another, this session with real fixture data (no seeding needed — each screen has ≥0 real backing rows): (1) **`/wallet/declarations`** (`DeclarationsListScreen.tsx:67`) — `listDeclarations()` typed `Declaration[]`, live `GET /declarations/list` actually returns `{"declarations":[...]}"` (a *third*, uniquely-named envelope key, distinct from both `{items:[...]}"` and `{data:[...]}"`) — crashes with a real declaration (id 23) present. (2) **`/wallet/surrenders`** (`TokenSurrendersListScreen.tsx:67`) — `listSurrenders()`-equivalent typed as an array, live `GET /token-surrender/list` returns `{"data":[...],"total":...,"skip":...,"limit":...,"page":...}` (a *fourth* distinct envelope shape). (3) **`/pts` History tab** (`PtsDashboardScreen.tsx:384`) — `getPtsHistory()` typed `PtsConversion[]`, live `GET /pts/history` returns the standard `{items:[...]}"` envelope. (4) **`/points/activities` "My activity" tab** (`ActivityRewardsScreen.tsx`) — `getMyActivities()` typed `ActivityLogEntry[]`, live `GET /activity-rewards/activities/me` also returns `{items:[...]}"`. Every one of these takes down the **entire app** (not just the widget) via React Router's unhandled-error boundary, because no app-level `ErrorBoundary`/`errorElement` exists anywhere (the same gap TR-184 already flagged). (5) **`/admin/proposals`** (`AdminProposalsScreen.tsx:189`) — `getProposals()` (`FrontEnd/src/api/proposals.ts:23-25`) typed `Proposal[]`; `proposals_GET.xs` (lines 21-37) issues a paginated `db.query` with `paging: {...totals:true}` and returns the raw envelope with `response = $rows` — i.e. the top-level response body itself *is* `{items:[...], itemsTotal:N, curPage:1,...}`, with no wrapper key at all (a *fifth* distinct shape: no key, direct envelope). `AdminProposalsScreen.tsx:189` calls `(proposals.data ?? []).map(...)` directly on that envelope object, producing `TypeError: (proposals.data ?? []).map is not a function` and crashing the entire app the instant an admin opens the Proposals queue — **no admin can ever see, accept, request changes on, or reject a single marketplace proposal.** Combined with TR-184, TR-195, and TR-203 (the sibling silent-failure variant), that is **seven confirmed broken-listing screens from one systemic pattern** found in a single session, strongly suggesting the same bug exists on other untested list screens too. Two-part fix needed: (a) audit and unwrap every `*.ts` API list-wrapper against its live envelope shape (three different envelope key names were found: `items`, `data`, and `declarations` — there is no single consistent convention to rely on), and (b) add a root-level `ErrorBoundary`/`errorElement` in the router so any future instance of this bug degrades to one broken widget instead of the whole app. | Live navigation 2026-07-20 to `/wallet/declarations`, `/wallet/surrenders`, `/pts` (History tab), `/points/activities` (My activity tab), all as `VGC50` (local dev); each response shape independently confirmed via direct `curl`; source confirms the array-typed wrapper in each case | Open |
| TR-195 | Critical | Frontend | `/market/propose` crashes the entire app for every member, always — same envelope-vs-array bug class as TR-184 | `getProposals()` (`FrontEnd/src/api/proposals.ts:23-25`) types the response as `Proposal[]`, but the live `GET /proposals` endpoint returns the standard paginated envelope (`{itemsReceived:0,...,items:[]}`, confirmed live). `ProposeItemScreen.tsx:214` calls `proposals.data.map(...)` **with no optional chaining at all** (worse than TR-184's `?.map`), so it throws unconditionally on every render, with zero existing proposals or many. Confirmed live: navigating to `/market/propose` as `E2E Adult A` immediately crashed to the unhandled-error boundary (`TypeError: proposals.data.map is not a function`), taking down the entire app view exactly like TR-184 — no app-level `ErrorBoundary`/`errorElement` exists anywhere in the router, so this single component bug is unrecoverable without a manual reload. This is the third instance this session of the same systemic pattern (raw paginated envelope vs. an array-typed FE wrapper) — see TR-184 (guardian approvals) and the general pattern noted in §18. **No member can currently propose a marketplace item through the UI at all.** **Wider blast radius confirmed 2026-07-20**: `TeacherDashboardScreen.tsx`'s empty state explicitly instructs "Propose a course ticket from the marketplace to get started" — Education course creation has no separate entry point and depends entirely on this same broken screen, so this one crash also blocks the entire Education module's course-creation pathway, not just item proposals. Fix: unwrap `.items` (same fix needed for TR-184), and add a root-level error boundary so this entire bug class stops taking down the whole app. | Live navigation 2026-07-20 to `/market/propose` as `VGC50` (local dev); `GET /proposals` response shape confirmed via direct API; `ProposeItemScreen.tsx`/`proposals.ts` source | Open |
| TR-194 | Critical | Backend | Marketplace — cancelling a non-token order refunds the buyer in the wrong currency, permanently | Confirms the §18 "Non-token Marketplace flows may settle in Token" hypothesis with concrete, reconciled financial evidence. `marketplace/orders/id/cancel_POST.xs` line 35 hardcodes `currency: "token"` on the refund's `mutate_wallet` call — the order's actual `currency` field (which can be `INR`/`VGC_TOKEN`/`VGC_POINTS`) is never read anywhere in the function. Confirmed live: `E2E Adult A` (`VGC50`) bought item id 2 (currency `VGC_POINTS`, 6.95) via Buy Now — points wallet correctly debited 900→893.05 (see TR-189's sibling finding FIN-003). Cancelling that order (pre-POD, the only allowed cancel state) returned `{"cancelled":true,"refunded_tokens":6.95}` — the points wallet was **never credited back** (stayed at 883.27 after a second, unrelated purchase) while the **token** wallet, previously exactly `0`, jumped to `6.95` out of nowhere. The member effectively lost 6.95 real points permanently and received 6.95 unpaid-for tokens instead — a genuine currency-substitution financial defect, not merely a display issue. A second cancel attempt on the same order was correctly blocked (`400`, no double-refund), so this is a single-currency-wrong-target bug, not a double-spend risk — but it still corrupts every currency-tracked reconciliation for any cancelled non-token order. **Confirmed by source the same session (not yet live-reproduced): `marketplace/orders/id/mark_received_POST.xs` hardcodes `currency:"token"` twice** (lines 102 and 118 — the seller-credit and admin-fee-split legs of the normal successful-purchase settlement path), meaning this is not just a cancellation edge case: **every completed order for an item priced in INR or Points would pay the seller and admin in tokens instead of the item's real currency.** This is the platform's primary revenue-settlement path, not a rare branch. **Now confirmed by source that the bug is total, not partial: all three admin settlement endpoints hardcode `currency: "token"` on every single `mutate_wallet` call in every branch** — `admin/admin/marketplace/orders/auto_settle_POST.xs` (lines 66, 81), `admin/admin/marketplace/orders/id/settle_POST.xs` (lines 75, 90), and `admin/admin/marketplace/orders/id/resolve_dispute_POST.xs` (all nine `mutate_wallet` calls across its `full_refund`/`partial_refund`/`vendor_favour` branches: lines 87, 132, 145, 160, 217, 232). None of these five files ever reads `$order.currency` or the underlying item's `currency`. This means **every settlement path in the entire marketplace — auto-settle, manual settle, and all three dispute resolutions — pays proposer/admin/buyer in VGC_TOKEN regardless of the order's real currency**, not just the cancel/mark-received paths already confirmed live. Not yet live-reproduced for the admin-side paths (blocked pending a non-token disputed/pod_submitted order fixture), but the pattern is now source-confirmed as complete and systemic across the whole settlement surface. **Separately (lower severity, UX only, no financial risk since the backend correctly blocks the retry): the "Cancel order" button remains visible and clickable after the order reaches the terminal `Cancelled` status** — clicking it again correctly 400s server-side with a reused-but-technically-inaccurate message ("Order can only be cancelled before proof of delivery is submitted"), so no defect there beyond the stale button. | Live reproduction 2026-07-20: order for `VGC50` item id 2 (`VGC_POINTS`), cancelled, wallets re-queried before/after (`points` unchanged at 883.27, `token` 0→6.95); `cancel_POST.xs` source confirms the hardcoded `currency:"token"` | Open |
| TR-193 | Medium | Frontend | Marketplace item detail — price is never shown with a currency unit, and the currency used for formatting is hardcoded wrong | `ItemDetailScreen.tsx` calls `formatAmount('VGC_TOKEN', data.price)` and `formatAmount('VGC_TOKEN', total)` unconditionally — always passing the literal `'VGC_TOKEN'` regardless of the item's actual `data.currency` field (which can be `INR`/`VGC_TOKEN`/`VGC_POINTS` per the live marketplace, confirmed live: item id 2 "lemongrass salmonberry" has `currency:"VGC_POINTS"`). Two compounding issues: (1) `formatAmount()` itself never appends a currency label/suffix for non-INR currencies (only `formatSigned()`, used elsewhere, does that) — so **no marketplace item's price ever shows a currency unit on this screen**, a bare number like `6.95` regardless of whether it's tokens, points, or rupees; (2) for a genuinely **INR-priced** item, the hardcoded `'VGC_TOKEN'` argument means the price would never get the ₹-symbol/2-decimal INR formatting `formatAmount` provides for that branch — an INR item's price would render as a plain number instead of `₹X.XX`. Confirmed live via the item detail page for id 2 (points-priced): "6.95" shown with zero currency indicator anywhere (title, quantity, total, Buy/Add-to-cart buttons). Violates the plan's explicit UI standard (§9.1: "Monetary values always state currency"). Recommend passing `data.currency` instead of the hardcoded literal, and ensuring `formatAmount` (or a wrapping component) always renders a visible unit/label. | Live `/market/item/2` inspection 2026-07-20 as `VGC50` (local dev); `ItemDetailScreen.tsx`/`currency.ts` source | Open |
| TR-192 | Medium | Backend | Points transfer — recipient gets zero notification of an instant, irreversible incoming transfer | `points_transfer/points_transfer_POST.xs` (162 lines) contains no `emit_notification` or `util.send_email` call anywhere — confirmed by full-file grep. Live end-to-end test: sent 100 points from `E2E Adult A` (`VGC50`) to `E2E Adult B` (`VGC51`) through the real `/points/send` UI; the transfer itself worked perfectly (reconciled exactly: sender 1000→900, recipient 0→100, passbook entry correct with remark/timestamp) — but `GET /notifications` for the recipient immediately after returned zero items. The confirm screen itself warns "Points are transferred instantly and cannot be reversed," yet the recipient has no in-app or email signal that money silently arrived in their account. Recommend an `emit_notification` call (and ideally email) to the recipient on every successful transfer. | Live reconciled transfer 2026-07-20 (`VGC50`→`VGC51`, disposable, 100 pts) via `/points/send`; recipient notifications checked immediately after; full-file source grep confirms no notification/email code path exists | Open |
| TR-191 | High | Both | Admin — "Adjust Wallet" (manual credit/debit) has never worked through the UI; wrong field name | Same admin screen, second broken tool. `adminAdjustWallet()` sends `{member_id, currency: string, amount, reason}`, but `admin/wallets/adjust_POST.xs` declares `enum wallet_type {values:["INR","VGC_TOKEN","VGC_POINTS"]}` — the field is named `currency` instead of `wallet_type` (and would also need the uppercase/underscore casing, the same enum-mismatch family as TR-181). Confirmed live in the same session as TR-190: entering `1000`/reason and clicking "Adjust" for `E2E Adult A`'s Points wallet returned `400 "Missing param: wallet_type"`. Combined with TR-190, **the admin panel currently has no working way at all to manually credit or debit a member's wallet** — both mutation tools on this screen are broken. | Live reproduction 2026-07-20 via `/admin/wallets` as 2FA-verified `VGC53`; `AdminWalletsScreen.tsx`/`admin.ts`/`adjust_POST.xs` source | Open |
| TR-190 | High | Both | Admin — "Award Points" (Constitutional/Promotional) has never worked through the UI; wrong field name and wrong casing | `AdminWalletsScreen.tsx`'s `adminAwardPoints()` call sends `{member_id, amount, type: 'Constitutional', reason}`, but `admin/points/award_POST.xs` declares `enum provision_type {values:["constitutional","promotional"]}` and checks `$input.provision_type == "promotional"`/`"constitutional"` (lowercase) — the field is named `type` instead of `provision_type`, **and** its value is capitalized (`'Constitutional'`) where the backend enum only accepts lowercase. Confirmed live: clicked "Constitutional" (already the default-selected state) then "Award Points" for `E2E Adult A` in the real admin panel (test-admin `VGC53`, legitimate 2FA session) — `400 "Missing param: provision_type"`. This is a day-one bug in a core admin governance action (SRS §5.2 Constitutional/Promotional point minting) that has apparently never functioned through the product. | Live reproduction 2026-07-20 via `/admin/wallets` as 2FA-verified `VGC53`; `AdminWalletsScreen.tsx`/`admin.ts`/`award_POST.xs` source | Open |
| TR-189 | Critical | Backend | Declarations — the entire INR declaration pipeline is unreachable end-to-end; no member-submitted declaration (Donation/Grant/Sponsorship/Investment/Token Purchase) can ever reach admin review | Confirms the §18 "Declaration lifecycle may be unreachable" hypothesis with concrete runtime evidence. `create_declaration.xs` (the shared insert helper used by every creation path) always writes `status:"draft"`. `inr_forms/id/submit_POST.xs` — the only endpoint that could ever move a declaration forward — requires `precondition ($declaration.status == "pending")` before it will do anything, **and even in its success path never writes any status change at all** (no `db.edit` anywhere in the file). Since nothing ever sets status to `"pending"`, this precondition can never be satisfied by a declaration that went through the real creation flow, so `submit` always returns `400 "Declaration has already been submitted or reviewed."` — the exact message is actively misleading, since the declaration was never submitted at all. `admin/declarations/id/verify_PATCH.xs` independently also requires `status == "pending"`, so even if a declaration somehow reached that state, both halves of the pipeline gate on a status neither half can ever produce. Confirmed live end-to-end via the real UI: filled and submitted a complete Donation declaration as `E2E Adult A` at `/wallet/declare` (name/contact/email/Member-ID auto-fill all correct, ₹500 UPI payment) — `POST /declarations` succeeded (`200`, id 23, `status:"draft"`), then `POST /declarations/23/submit` immediately `400`'d with the misleading already-submitted message. This explains why prior admin-panel testing (TR-161) appeared to work: it exercised 22 **pre-seeded** declarations already sitting at `status:"pending"` in the database, never a declaration that passed through this actual create→submit code path — the live user-facing feature has apparently never worked. Fix: `submit_POST.xs` needs to both gate on `status=="draft"` (not `"pending"`) and actually write `status:"pending"` on success. | Live end-to-end reproduction 2026-07-20 via `/wallet/declare` as `VGC50` (declaration id 23, disposable, left `draft`, harmless/zero financial impact); `create_declaration.xs`/`submit_POST.xs`/`admin/declarations/id/verify_PATCH.xs` source | Open |
| TR-188 | Medium | Both | Verify-email — the "code" is actually a full UUID, but the field is labelled/keyboarded for a 6-digit number | `VerifyEmailScreen.tsx`'s code field is `<Field label="Verification code" inputMode="numeric" autoComplete="one-time-code" placeholder="6-digit code" .../>`, but every verification token issued this session (`resend_verification_POST.xs`/`verify_email_POST.xs`, both via `security.create_uuid`) has been a full 36-character UUID (e.g. `c0cab581-c852-4208-b487-5b0f6d182013`), never a 6-digit number. On a real mobile device (this is a mobile-first PWA), `inputMode="numeric"` requests a numeric-only virtual keyboard, which has no letters or hyphens — a user could not manually type the actual code they were emailed without the OS offering a way to switch keyboards, which most won't know to do. The placeholder text itself is also simply wrong. Confirmed via every verification email observed this run (Adult A/B/C/split-brain/test-admin/both minors — 10+ real codes, all UUIDs, zero 6-digit codes). Recommend either shortening the backend token to a real 6-digit OTP (matching the label/keyboard), or fixing the field to `inputMode="text"`/no restrictive mode and correcting the placeholder. | Cross-referenced every verification-code email delivered this session vs. `VerifyEmailScreen.tsx` source (`inputMode="numeric"`, placeholder `"6-digit code"`) | Open |
| TR-187 | Critical | Backend | Password reset — the emailed magic link is completely non-functional: wrong domain, wrong page, and the token/email are concatenated with no separator | `authentication/reset/request_reset_link_GET.xs` and its callee `Quick Start/generate_magic_link.xs` are both still tagged `["xano:quick-start"]` — unmodified Xano scaffold boilerplate, never adapted to this app. The link is built as `$env.$api_baseurl |concat:"1_start_here_demo_page#/update-password":"/" |concat:$token:"?magic_token=" |concat:$email:""` — i.e. it points at `https://x8ki-letl-twmt.n7.xano.io/api:L9PANOan/1_start_here_demo_page#/update-password?magic_token=<TOKEN><EMAIL>` (the **raw Xano API host and Xano's own generic quick-start demo page**, not the VGC frontend's `/reset` route at all), and the final `concat` uses an **empty separator**, so the token and the recipient's email are jammed together into one unparseable string with no delimiter between them. Confirmed live end-to-end: requested a reset for verified `E2E Adult B` (`VGC51`) via the real `/forgot-password` UI, retrieved the actual delivered email, and found exactly this — `href="https://x8ki-letl-twmt.n7.xano.io/api:L9PANOan/1_start_here_demo_page#/update-password?magic_token…fec2ca-e1d2-4110-bc41-47ff58d23f24vgcreinventinggaming+e2e0719b@gmail.com"` (token and full email address concatenated directly, no `&email=` or similar). No real member could ever complete a password reset by clicking the emailed link — it does not lead to this application at all, and even a technically-inclined user manually navigating to `FrontEnd`'s own `/reset?token=…` would need to somehow separate the token from the trailing email text with no delimiter to key off. `ResetPasswordScreen.tsx` does correctly check both `?token=` and `?magic_token=` param names (a prior partial fix attempt), which is moot given the link never points at this app's domain in the first place. This is a Critical-severity account-recovery blocker — the entire self-service password reset feature is unusable for any real member. Secondary, minor finding on the same screen: when the exchange genuinely fails (confirmed on production, `?token=` set to a garbage value), `ResetPasswordScreen.tsx` surfaces the *raw* backend validation string ("email is required but not provided") directly to the member instead of its own friendly fallback copy ("This reset link has expired or already been used.") — `errorMessage()` prefers the live API message over the fallback, so any raw backend wording (a §9.6/§18 "no raw Xano error/development jargon" violation) leaks straight through. On local dev only (not production — confirmed by side-by-side comparison), the same failure instead leaves the screen stuck on "Verifying your link…" forever with no error and no escape route; production correctly shows the error+recovery link, so this specific stuck-spinner sub-case is a React 18 `StrictMode` dev-only artifact, not filed as a separate defect. **Addendum 2026-07-21** (backend endpoint sweep): confirmed the exact raw text via direct `curl` — a bare `GET reset/request-reset-link` (no email) returns `{"code":"ERROR_FATAL","message":"email is required but was not suppiled. ","payload":""}` (note the typo, "suppiled") as an `ERROR_FATAL`/`500`, not the standard `ERROR_CODE_INPUT_ERROR`/`400` every other required-field omission in this codebase returns — both `request_reset_link_GET.xs` and `reset/magic-link-login_POST.xs` use manual `precondition`-with-custom-message validation instead of Xano's normal required-input declaration, which is why they diverge from the rest of the API's consistent 400-on-missing-param behaviour. Cosmetic relative to the link itself being broken, but confirms this pair of files is generally lower quality than the rest of the endpoint surface. | Live reset request 2026-07-20 via `/forgot-password` for `VGC51`; actual delivered email inspected via Gmail `get_message`; `request_reset_link_GET.xs`/`generate_magic_link.xs` source confirms root cause | Open |
| TR-186 | High | Backend | Admin login — account and admin-flag enumeration via three distinct error messages | `admin/2fa/login_POST.xs` returns three distinguishable errors depending on account state: `"Account not found."` (unregistered email), `"Not an admin account."` (real member, `role_flags.is_admin` false) and `"Invalid password."` (real admin, wrong password) — confirmed live via the browser at `/admin/login` with a nonexistent address, then `E2E Adult B`'s real non-admin credentials, then the real disposable admin with a deliberately wrong password. This lets an attacker enumerate registered emails and, more specifically, **identify which accounts are admin-flagged** — a materially higher-value target list than ordinary user enumeration — before ever attempting a password guess. The ordinary member `/login` endpoint had this exact class of bug and was fixed under TR-146 (byte-identical generic error for unknown-email vs wrong-password); the admin login path was apparently never given the same treatment. Recommend collapsing all three cases to one generic message, mirroring `login_POST.xs`'s fix. | Live browser reproduction 2026-07-20 at `/admin/login` (local dev): three distinct error strings for three distinct account states | Open |
| TR-185 | Critical | Backend | Guardian approval account creation is fatally broken — approving a minor has apparently never succeeded even once in this workspace | `guardian_approvals/id/respond_POST.xs`'s approve branch calls `db.add user` without setting `member_id` at all (defaults to empty string `""`) — unlike `signup_POST.xs`, which was specifically patched (see historical BG-2) to insert a temporary `security.create_uuid` value first, then rewrite it to `"VGC<id>"`, precisely because `member_id` carries a UNIQUE index and a second empty-string insert collides. This same fix was never applied to the guardian-approval path. Confirmed live: after correctly clearing the IDOR guard (see TR-184's note — `403` for the wrong guardian), a legitimate approval by the correct guardian (`E2E Adult A`/`VGC50`) for a real pending request (id 1, minor `E2E Minor Approved`) returned `500 ERROR_FATAL "Duplicate record detected. Please check your input and try again."` Root cause confirmed by direct lookup: pre-existing real user id 19 (`member_id:""`, a long-lived pre-2026-07-19 account, clearly not a disposable test row) already occupies the empty-string slot, so **every** guardian approval attempt on this workspace will hit this collision and fail — this is not specific to this run's fixture. The `guardian_approvals` row was left safely in `status:"pending"` (the failing `db.add user` is the first mutating statement in that branch, so nothing partial/orphaned was created this time — no wallets, no setup-token, no minor account), but no future approval can succeed until this is fixed, since there is no `db.transaction` wrapper and no retry-safe id-collision handling. Combined with TR-183 (guardian never emailed) and TR-184 (approvals screen crashes, and even a fixed screen would send the wrong payload shape), **the entire guardian/minor onboarding pathway is non-functional at every layer** — this appears to be an SRS §2.1.2 safeguarding feature that has never actually worked end-to-end. The pending disposable request (id 1) was left in place rather than retried, since retrying is guaranteed to fail identically until the backend is fixed. | Live approval attempt 2026-07-20 as guardian `VGC50` against real pending request id 1; root-cause lookup via `searchTableContent` confirming pre-existing user id 19 with `member_id:""`; `respond_POST.xs` vs `signup_POST.xs` source comparison | Open |
| TR-184 | Critical | Frontend | Guardian Approvals screen (`/profile/guardian-approvals`) crashes the entire app for every guardian, always | `getGuardianApprovals()` (`FrontEnd/src/api/auth.ts:141-143`) calls `request<GuardianApproval[]>(..., '/guardian-approvals/me')`, typing the response as a bare array — but the live endpoint returns the standard paginated envelope (`{itemsReceived, curPage, nextPage, ..., items:[...]}`), confirmed via direct `GET /guardian-approvals/me` (`{"itemsReceived":1,...,"items":[{...}]}`). `GuardianApprovalsScreen.tsx:55,58` then calls `approvals.data.length`/`approvals.data?.map(...)` directly on that envelope object, which has neither — `.map` throws `TypeError: approvals.data?.map is not a function` on **every** render, with **zero** pending approvals or many. Confirmed live: navigating to `/profile/guardian-approvals` as guardian `E2E Adult A` (who has exactly one real pending request, id 1) instantly crashed to React Router's unhandled-error boundary — the raw `TypeError` and full stack trace rendered directly in place of the app (no app-level `ErrorBoundary`/`errorElement` exists anywhere in the router, confirmed by the error overlay's own "Hey developer" note pointing this out), leaving the guardian with no way to view or respond to the request at all through the UI. This is the concrete, naturally-occurring confirmation of the §18 "Error white-screen risk" hypothesis (no controlled fault injection was needed — an ordinary pending guardian request is enough). Combined with TR-183 (no email sent), a guardian has **no working path at all** to discover and act on a minor's registration request — the in-app notification says "log in to respond," and the one screen that lets them respond is completely broken. This blocks the entire minor-onboarding pathway end-to-end. **Independently, the Approve/Reject action itself is also broken**: `respondGuardianApproval()` (`auth.ts:146-151`) POSTs `{approve: boolean}`, but the live endpoint (`guardian_approvals/id/respond_POST.xs`) requires `enum decision {values:["approve","reject"]}` — confirmed live, a direct `POST .../respond` with `{"approve":true}` returned `400 "Missing param: decision"`. So even after fixing the list-crash, clicking Approve/Reject as currently wired would still fail every time. Fix: unwrap `.items` (matching the pattern already fixed for orders/sales in TR-151), send `{decision: approve ? "approve" : "reject"}` instead of `{approve}`, and add a root-level `ErrorBoundary`/`errorElement` so isolated component bugs don't take down the whole app. | Live navigation 2026-07-20 as guardian `VGC50` with real pending request id 1 (local dev); `GuardianApprovalsScreen.tsx`/`auth.ts`/live envelope response | Open |
| TR-183 | High | Backend | Guardian workflow — guardian never receives the approval-request email, only the in-app notification | `guardian_registration_POST.xs` unconditionally calls `emit_notification` (confirmed working — see below) immediately followed, same stack, no precondition in between, by `util.send_email { subject: "VGC — Guardian Approval Required", to: $guardian.email }`. Live test: submitted a real guardian request for disposable `E2E Minor Approved` (age 17) naming verified guardian `E2E Adult A` (`VGC50`, `vgcreinventinggaming+e2e0719a@gmail.com`); confirmed via API that both the `guardian_approvals` row (`status:"pending"`, correct 7-day `expires_at`) and the in-app notification (`GET /notifications` → `event_type:"guardian_approval_pending"`, safe applicant-name-only body) were created correctly, but an exhaustive Gmail search (`in:anywhere`, no subject/date filter, and explicit `subject:guardian`) over the following minutes found **zero** matching email — compare with every other email type this run (verify-email codes, admin OTP codes) which all arrived within seconds. This is not the known BG-7 sandbox limitation (other `util.send_email` calls to the same address succeed). A guardian who is not actively checking the app has no signal a request exists at all, risking a silent 7-day expiry. | Live guardian-registration request 2026-07-20 (`guardian_approvals` id 1, notification id 5) vs. exhaustive Gmail search; `guardian_registration_POST.xs` source | Open |
| TR-182 | High | Both | Signup — minor guardian field asks for the "VGC Member ID" but actually requires the raw internal integer `id`, which no screen ever surfaces (compounds TR-175) | `guardian_registration_POST.xs` declares `int guardian_member_id` (raw integer), and `SignupScreen.tsx` sends the field's raw trimmed text with zero format conversion. The field is labelled "Parent / guardian **VGC Member ID**" — the platform's public identifier is always the `VGC<n>` format (`VGC50` etc.), used consistently everywhere else a member ID is shown (Declaration/Surrender forms, admin panels). Confirmed live: submitting the public-format `VGC999999` for a 17-year-old disposable signup (`E2E Minor Approved`) was rejected client-side with `"Value is not a valid integer."` — the field silently expects the bare number instead. Combined with TR-175 (no screen shows a member their own `member_id`, let alone their raw `id`), a real guardian has **no in-product way to discover the value this required field needs** — the field's own label actively points them at the wrong format. `SignupScreen.tsx` also still carries a stale code comment ("applicants must supply a guardian's email") left over from before the `guardian_member_id` design was corrected (see `FrontEnd/session_log.md` DOC-1) — harmless but worth cleaning up alongside a real fix. Recommend either accepting/normalising `VGC<n>` input, or relabelling the field and exposing the raw id somewhere reachable. | Live signup attempt 2026-07-20 with disposable `vgcreinventinggaming+e2e0719minor1@gmail.com` (age 17) against local dev; `guardian_registration_POST.xs`/`SignupScreen.tsx` source | Open |
| TR-181 | **Critical (revised up from Medium)** | Backend | `GET /user/profile` returns null for almost every field, not just wallets — root-caused to a platform-wide `$auth.<field>` resolution bug (see TR-204) | **Revised 2026-07-20 after discovering TR-204.** Originally logged as a narrow wallet-enum-casing bug; direct live re-test shows the damage is far broader. `profile_GET.xs`'s entire response block reads `$auth.member_id`, `$auth.email`, `$auth.name`, `$auth.mobile`, `$auth.role`, `$auth.suspended`, `$auth.kyc_verified` etc. directly — confirmed live for `E2E Adult A`: `GET /user/profile` returns `200` but with `member_id:null, email:null, full_name:null, phone:null, role:null, kyc_verified:null` — literally every field except a hardcoded `status:"active"` and the (separately, originally-diagnosed) zero-default wallet block. This is the **silent-null** manifestation of the same `$auth` context bug that causes **hard 500 crashes** elsewhere (TR-204) — evidently direct `$auth.<column>` access resolves to `null` when read as a plain value but throws when used inside a `precondition` boolean. **Also live-facing, contrary to the original write-up**: `EditProfileScreen.tsx` reads `profile.data.name`/`.display_name`/`.bio`/`.mobile`, none of which exist on the backend's actual keys (`full_name`/`phone`, no `display_name`/`bio` at all) — a second, independent field-name mismatch on top of the `$auth` bug, so the Edit Profile screen's initial field population is broken twice over. Escalated to Critical given the confirmed live user-facing impact and the platform-wide root cause. | Live `GET /user/profile` response for `VGC50` (2026-07-20, all fields null except hardcoded/defaulted ones); `GET /wallets/me` shape comparison; `EditProfileScreen.tsx`/`profile.ts`/`profile_GET.xs` source | Open |
| TR-204 | Critical | Backend | Platform-wide: direct `$auth.<field>` access (beyond `$auth.id`) is broken — silently `null` as a plain value, hard `500` when used in a `precondition` — breaking admin Verify/Reject/Complete/Fulfill actions for every caller | Discovered while investigating why "Verify" on a declaration in the live admin panel produced a raw `"Unable to locate auth: role"` error instead of the expected business-rule message. Root cause: `admin/declarations/id/verify_PATCH.xs` and `reject_PATCH.xs` both gate on `precondition ($auth.role == "admin")` — but `$auth.role` cannot be resolved in this context and throws a hard `500 ERROR_FATAL "Unable to locate auth: role"` **for every caller, admin or not, regardless of authentication method** (confirmed identically via both the 2FA-issued browser session and a plain ordinary-`/login` token for the same account). `grep` for the identical `$auth.role` pattern found **six** affected files: `admin/declarations/id/verify_PATCH.xs`, `admin/declarations/id/reject_PATCH.xs`, `admin/token_surrenders/id/complete_PATCH.xs`, `marketplace/admin/orders/id/fulfill_PATCH.xs`, `marketplace/admin/orders_GET.xs`, and `user_profile/profile_GET.xs` (which uses `role` as a plain value rather than in a `precondition`, so it degrades to silent `null` instead of crashing — see TR-181's revision). **No admin can currently verify or reject an INR declaration, complete a token surrender, or fulfil/list marketplace admin orders through this specific code path** — confirmed live for declaration verify/reject (both `500`) **and now also live-confirmed for `admin/token_surrenders/id/complete_PATCH.xs`** (clicking "Complete Surrender" on a real pending surrender in `/admin/declarations` → "Token Surrenders" tab produces the identical `"Unable to locate auth: role"` inline error). **Live-reconfirmed 2026-07-21 for the final two**: direct `curl` with a valid admin token against `GET admin/orders` and `PATCH admin/orders/1/fulfill` both return the identical `500 ERROR_FATAL "Unable to locate auth: role"` — all 6 affected files are now live-confirmed, none remain source-only. This is likely one instance of a wider pattern: any endpoint written assuming direct `$auth.<column>` access beyond `id` may be silently or fatally broken; a full `grep -rn '\$auth\.'` sweep excluding `\$auth\.id` is recommended before relying on any other endpoint's use of `$auth` fields. | Live reproduction 2026-07-20: `PATCH /declarations/23/verify` and `/reject` both `500 "Unable to locate auth: role"` for `VGC53` via both 2FA and ordinary-login tokens; live UI reproduction of `PATCH /token-surrenders/20/complete` (surrender #20, `Test User 2`/VGC-00005) producing the same error; `grep -rl '\$auth\.role\b' XANO/api/` confirms 6 affected files. **2026-07-21**: direct `curl` with a valid admin token confirms the identical `500` on both `GET admin/orders` and `PATCH admin/orders/1/fulfill` | Open |
| TR-205 | Critical | Backend | Token-surrender completion debits **zero** tokens from the member's wallet instead of the real surrendered amount — masked today by TR-204's precondition crash, but a live money-for-nothing exploit the moment TR-204 is fixed | `admin/token_surrenders/id/complete_PATCH.xs:52-53` computes the debit as `var $tokens { value = $surrender.tokens_to_surrender ?? $surrender.vgc_token_amount }`. The `token_surrenders` table schema (`XANO/table/token_surrenders.xs:18`) declares `tokens_to_surrender` as `decimal?` with **default `0`, not null**, and `token_surrenders/create_POST.xs` (the only member-facing creation path) never sets `tokens_to_surrender` at all — it only writes `vgc_token_amount`. Because Xano's `??` only falls through on a true `null`, and the field's actual stored value is `0` (a real number, not null), `$tokens` always resolves to **`0`** for every surrender ever created through the product, seed data included (confirmed via direct table read: `tokens_to_surrender:0` on all 20 existing rows regardless of `vgc_token_amount`). Consequences once TR-204 is fixed and this code path becomes reachable: (1) the wallet debit `data = {balance: $wallet.balance - $tokens}` subtracts 0 — **the member keeps 100% of their surrendered VGC tokens**; (2) the surrender is still marked `status:"completed"`; (3) the `event_log` insert records `amount: 0`, masking the discrepancy from any audit trail built on that log; (4) if the INR-equivalent payout to the member is triggered by `status=="completed"` anywhere downstream (not yet located/tested), this becomes a real free-money exploit — INR paid out, tokens never actually removed. Same root-cause shape as TR-149/193/196 (frontend/backend key-name drift) but here it's an intra-backend key drift between the write path and the read path, and it corrupts a financial ledger rather than crashing a UI. | Table read via Xano MCP `getTableContent` on `token_surrenders` (table_id 839690): all 20 rows show `tokens_to_surrender: 0` with nonzero `vgc_token_amount`; source review of `complete_PATCH.xs` lines 51-70, `token_surrenders.xs` schema line 18, `create_POST.xs` lines 40-49 (never sets `tokens_to_surrender`) | Open |
| TR-217 | High | Backend | Confirmed IDOR: any authenticated member can read every applicant's private contract bids/proposals for any contract, including ones they have no relationship to | `contracts/contracts/{id}_GET.xs` declares only `auth = "user"` — no `precondition` anywhere in the file compares `$auth.id` to `giver_member_id`, `taker_member_id`, or any applicant's `applicant_member_id` before returning data. The response includes the full `applications` array (lines 118-136), each entry carrying `proposed_price_points` (the applicant's private bid/price), `application_text`, `detailed_proposal` (a long-form private write-up), applicant identity, and rating history — all keyed only by a small, sequential, trivially-enumerable `contract.id`. Contrast with sibling files in the same directory that get this right: `contracts/applications/app_id/messages_GET.xs` and `dispute_messages_GET.xs` both correctly gate on `$auth.id == giver_member_id || $auth.id == applicant_member_id` before returning any message content. **Live-reproduced**: logged in as `VGC52` (member id 52, fixture P-003, explicitly created this session as "unrelated third party for IDOR" — not the giver, not an applicant, not admin, no relationship whatsoever to the contract) and requested `GET /contracts/26` (a real, pre-existing, non-fixture disputed contract between "Visual Gorana" (giver, id 39) and "Varun ghavare" (taker/applicant, id 41)) — received a full `200` with the complete `applications` array, including Varun's `proposed_price_points: 8500`, full `detailed_proposal` HTML, `application_text`, and rating counts. The frontend's `ContractDetailScreen` happens not to render the applications section for a non-giver caller, but that is purely cosmetic — the private data is already present in the raw HTTP response body, fully readable via browser devtools/network tab or a bare `curl` with any valid member token, by anyone able to guess or enumerate a contract ID. This breaks the platform's own competitive-bidding privacy model: a rival applicant (or anyone at all) can read every other applicant's exact price and full pitch for any contract, letting them undercut bids or plagiarize proposals with zero technical effort. | Live reproduction 2026-07-20: `GET /contracts/26` as `VGC52` (id 52, unrelated third party) returns full `applications[]` with applicant `proposed_price_points`/`detailed_proposal`/`application_text` in the raw response body (captured via Playwright network inspection, response-body text quoted above); source review of `contracts/contracts/id_GET.xs` (no ownership `precondition` anywhere in the file) vs. correctly-gated siblings `messages_GET.xs`/`dispute_messages_GET.xs` in the same directory | Open |
| TR-216 | Medium | Backend | Unauthenticated endpoint lets anyone trigger a real email send to any member by guessing/enumerating user IDs | `authentication/message/send_welcome_email_POST.xs` declares no `auth` line at all (every other endpoint checked this session declares `auth = "user"` at minimum) — it is publicly callable with just an `int user_id` and no session token. It looks up the target user, then calls `util.send_email` for real via Xano's own mail infrastructure with a hardcoded "Welcome to Our Service" template. Tagged `tags = ["xano:quick-start"]` and never called from anywhere in the frontend (`signup_POST.xs`, the actual registration flow, never invokes it either) — it is leftover scaffolding, not a wired feature, but it is live and reachable. Impact: an unauthenticated caller can enumerate sequential `user_id` values and force a real email to be sent to any member repeatedly (spam/harassment vector, and minor platform email-reputation/deliverability risk from repeated unsolicited sends), with no rate limit observed on this specific route (distinct from the registration/resend-verification endpoints TR-140 confirmed ARE rate-limited). Not exploited beyond a single-call reachability check — no repeated sends triggered against a real account. | Source review 2026-07-20: `send_welcome_email_POST.xs` lines 1-5 (no `auth` declaration, unlike every sibling endpoint in the same file); confirmed never referenced by `signup_POST.xs` or any frontend source (`grep -r` across `FrontEnd/src` for `welcome` and `send_welcome_email` found no real caller) | Open |
| TR-215 | High | Both | Notification Preferences screen is completely non-functional in both directions — frontend and backend were built to two incompatible data models that never integrated | Frontend (`NotifPreferencesScreen.tsx`, `api/notifications.ts` lines 34-43) expects a **list of per-event toggle rows** — `NotificationPreference[]`, each with `event_type`/`in_app`/`email`/`label?` — grouped and rendered as one card per event category with individual In-App/Email toggles. The live backend (`notifications/preferences_GET.xs`) instead returns a **single global object** — `{email: true, in_app: true, per_event: {}}` — with only two blanket toggles and a `per_event` map that is declared in the schema but never populated with real per-event-type entries anywhere in the codebase (grep found no writer that ever sets specific keys inside it). Because the response is neither a bare array nor `{preferences: [...]}` (the two shapes `getNotificationPreferences()` checks for), it always falls through to `?? []`, so the screen always renders **"No preferences — The server returned no configurable preferences."** — confirmed live as `E2E Adult A`/`VGC50` and as 2FA-verified `VGC53`, both showing the identical empty state regardless of account. The break is bidirectional: even if GET were fixed to return real rows, `updateNotificationPreferences()` sends `{preferences: [...]}` as the PATCH body, but `preferences_PATCH.xs`'s `input` block only declares top-level `email`/`in_app`/`per_event` — it has no `preferences` field at all, so a save would silently update nothing (every input is optional and none would be present). No member has ever been able to view or change a single per-event notification preference through this screen; the only real leverage a member has is the "Critical financial/security alerts cannot be disabled" notice, which is static copy, not backed by any actual per-event data. | Live reproduction 2026-07-20 at `/notifications/preferences` as both `VGC50` and 2FA-verified `VGC53`: identical "No preferences" empty state; `preferences_GET.xs` response shape vs. `api/notifications.ts` lines 34-43 vs. `NotifPreferencesScreen.tsx` lines 79-81; `preferences_PATCH.xs` input block (lines 5-9) vs. `updateNotificationPreferences()` body shape (line 50) | Open |
| TR-214 | Critical | Backend | Contract dispute chat treats "admin" as a single hardcoded member ID, not the platform's actual admin-role system — every admin account except one is denied access to the chat they're supposed to mediate | `contracts/applications/{app_id}/dispute-messages_GET.xs` (lines 37-43) and its sibling `_POST.xs` both define `$is_admin` as `$auth.id == $cfg.admin_member_id` — a direct comparison against the single `admin_member_id` value in `system_config` (currently `37`) — rather than using the platform's actual role-based admin check (`Quick Start/enforce_role`, used correctly by 30+ other admin endpoints this session, including several this admin persona has successfully passed). Confirmed live as 2FA-verified `VGC53` (member id `53`, a fully legitimate, 2FA-provisioned admin account — its own admin actions this session, e.g. `auto_settle_orders_batch`, correctly passed `enforce_role` checks): opening the "Dispute Chat" panel's Giver and Taker tabs on a real open dispute (`app_id` 16, contract 26, Giver "Visual Gorana"/39, Taker "Varun ghavare"/41) produced repeated `403` responses on `GET /contracts/applications/16/dispute-messages` for both threads — the admin resolving this dispute cannot see either party's messages, directly contradicting the endpoint's own doc comment ("Admin sees all messages"). Because `$is_admin` is false for this caller, and they are neither the giver nor the taker, the `precondition ($is_admin || $is_giver || $is_taker)` (line 53) fails outright. This is an architectural inconsistency: the codebase has two incompatible definitions of "admin" — the general role-flag system (`enforce_role`) used almost everywhere, and this one hardcoded single-ID comparison used only for dispute-chat read/write access — and any platform that provisions more than one admin account (which this one explicitly does, per the 2FA admin-setup flow tested earlier this session) will lock out every admin except whichever one happens to match `system_config.admin_member_id`. | Live reproduction 2026-07-20 at `/admin/contracts` as 2FA-verified `VGC53` (member id 53): opening "Dispute Chat" on a real open dispute (app_id 16) produces repeated `403` on `GET /contracts/applications/16/dispute-messages`; `system_config.admin_member_id` confirmed `37` via Xano MCP table read; `dispute_messages_GET.xs`/`_POST.xs` lines 37-57 | Open |
| TR-213 | Critical | Both | Admin Loans screen has no way to see any other member's loan requests — it lists only the logged-in admin's own loans, and no admin-wide loans list endpoint exists on the backend at all | `AdminLoansScreen.tsx` (line 72) calls `useMyLoans()` — the same member-scoped hook used by the ordinary member-facing loans screen — and openly says so in its own UI copy (line 78): "Showing loans visible to this account. In production, admin sees all member loans." Confirmed live as 2FA-verified `VGC53`: the screen renders "No loans" because the admin's own account has none, regardless of how many real pending loan requests exist from other members. `Approve`/`Reject`/`Write Off` buttons (lines 55-65) are correctly wired to `adminApproveLoan`/`adminRejectLoan`/`adminWriteOffLoan`, but since `loan.status === 'Pending'` can only ever be true for a loan that belongs to the logged-in admin's own account, **these controls can never be reached for a loan requested by any other member** — the entire admin loan-approval workflow is unreachable through the UI. Confirmed this is not merely a frontend wiring gap: `find`/`grep` across `XANO/api/admin` found no `admin/loans` (or equivalent) list `GET` endpoint at all — only per-loan `approve_POST.xs`/`reject_POST.xs`/`write_off_POST.xs` actions exist, each requiring an `id` the admin has no way to discover through this product. Combined with TR-201 (loan *request* creation itself broken by an `amount`/`amount_inr` field mismatch), **the Loans feature has never worked end-to-end between two different accounts in this build**: no member can successfully request one (TR-201), and even if one existed, no admin could ever find or act on it (TR-213). | Live reproduction 2026-07-20 at `/admin/loans` as 2FA-verified `VGC53`: "No loans" shown despite the screen's own disclaimer; `AdminLoansScreen.tsx` lines 72-78 confirm member-scoped query; `find XANO/api/admin -ipath "*loan*"` confirms no admin-wide list endpoint exists | Open |
| TR-212 | High | Frontend | Admin System Config screen's own placeholder JSON references a field (`surrender_rate`) that doesn't exist on the backend — following the UI's explicit guidance silently no-ops the exact setting it claims to fix | `AdminConfigScreen.tsx` (line 15) hardcodes the JSON textarea's initial content to `{"inr_per_token": 10, "surrender_rate": 8.5}`, with a caption (line 40) reading "Use this to set `inr_per_token` and `surrender_rate` (fixes BG-1). JSON format." The raw parsed JSON is sent verbatim as the PATCH body (`adminUpdateSystemConfig(JSON.parse(configJson))`, line 17) to `admin/system/config_PATCH.xs`, whose `input` block (lines 7-35) declares no `surrender_rate` field at all — the real schema/column is `tokens_per_inr_surrender_numerator`/`tokens_per_inr_surrender_denominator` (a fraction, e.g. `2/17`, on `system_config`, confirmed via direct table read), not a single decimal rate. Because Xano silently drops undeclared input keys rather than erroring, submitting the screen's own default placeholder **returns a `200` success with `inr_per_token` genuinely updated, while `surrender_rate` is discarded with zero effect and zero indication to the admin** — exactly reversing the placeholder's own promise ("fixes BG-1"). Any admin trusting this screen's built-in example to adjust the token-surrender rate will believe they've succeeded and will not have changed anything. **Live-confirmed**: submitted the screen's exact default placeholder as 2FA-verified `VGC53` — UI showed "Config updated." (a clean, unqualified success); a direct Xano MCP table read of `system_config` (table 840406) immediately after confirms `tokens_per_inr_surrender_numerator`/`_denominator` are untouched (`2`/`17`, unchanged) while `updated_at` genuinely advanced, proving the PATCH call succeeded and silently ignored the surrender-rate portion exactly as predicted from source. | Live reproduction 2026-07-20 at `/admin/config` ("Config updated." shown; `system_config` table read confirms `2`/`17` unchanged); source review: `AdminConfigScreen.tsx` lines 15, 17, 40 vs. `admin/system/config_PATCH.xs` lines 7-35 (no `surrender_rate` input) vs. live `system_config` table schema (`tokens_per_inr_surrender_numerator`/`_denominator`, confirmed via Xano MCP `getTables`) | Open |
| TR-211 | Medium | Backend | Admin Audit Log's "to" date filter silently excludes same-day entries — filtering "up to today" hides everything that happened today | `admin/admin/audit_log_GET.xs` (line 53) filters with `if ($input.to != null && $entry.created_at > $input.to) { $ok = false }`. The frontend's date-range picker (`<input type="date">`) sends a bare date like `"2026-07-20"`, which Xano's `timestamp` coercion resolves to midnight UTC (`2026-07-20T00:00:00Z`) — the *start* of that day, not its end. Any entry created later the same day has `created_at > $input.to` and is excluded. Confirmed live at `/admin/config`: the default date range shown was `2026-07-13` to `2026-07-20` (today), and the screen displayed "No audit entries for this period" — but a direct Xano MCP table read of `admin_audit_log` (table 840400) confirms **two real entries exist for today**, both created by this session's own admin actions (`game_create` at `2026-07-20T09:47:43Z`, `auto_settle_orders_batch` at `2026-07-20T11:23:42Z` — both from `RUN-060`/earlier phases, actor `VGC53`) — both excluded purely because they occurred after midnight UTC on the `to` date. Any admin using the (sensible, default) "up to today" range to review recent activity will see a false-empty result for the entire current day, every day, worldwide — the filter needs to treat a bare `to` date as end-of-day (`23:59:59.999`) rather than start-of-day. Data is not lost (widening the range to tomorrow's date recovers it), but the default UX for the single most common query ("what's happened today?") is silently wrong. | Live reproduction 2026-07-20 at `/admin/config`: date range `2026-07-13`–`2026-07-20` shows "No audit entries for this period"; direct Xano MCP table read of `admin_audit_log` confirms 2 real rows timestamped `09:47:43Z` and `11:23:42Z` on `2026-07-20` (both after the `to` boundary's implied midnight); `audit_log_GET.xs` line 53 | Open |
| TR-210 | High | Frontend | Admin Reports: 3 of 6 tabs (Market, Activity, Education) are permanently broken — the screen already has a shared date-range picker, but three of its own API wrapper functions silently drop it | `AdminReportsScreen.tsx` maintains a single `from`/`to` date-range state (lines 25-26) with visible date-picker inputs (lines 88-89) shared across all tabs, and correctly passes `{from, to}` to `adminReportFinancial()` (line 30). However `adminReportMarketplace()` (line 36), `adminReportActivity()` (line 42), and `adminReportEducation()` (line 54) are all called with **only `signal`** — their wrapper functions in `admin.ts` (lines 410-423) never even accept a `filters` parameter. The live backend for all three — `admin/reports/marketplace_GET.xs`, `activity_GET.xs`, `education_GET.xs` — declares `timestamp from` and `timestamp to` as **required, non-optional** inputs. Confirmed live as 2FA-verified `VGC53` at `/admin/reports`: Financial tab renders correctly with real aggregated data (points minted/burned matching this session's real activity); Gaming and Wallets tabs render correctly (no date-range dependency); **Market, Activity, and Education tabs all show "Couldn't load this — `Missing param: from`"** regardless of what dates are picked, because the picked dates are never sent. Half the report screen is unusable for any admin trying to review marketplace, activity, or education KPIs. | Live reproduction 2026-07-20 at `/admin/reports`, all six tabs, as 2FA-verified `VGC53`: Market/Activity/Education all `400 "Missing param: from"`; Financial/Gaming/Wallets all render real data; `AdminReportsScreen.tsx` lines 25-54 vs. `admin.ts` lines 410-423 vs. the three backend `_GET.xs` files' required `from`/`to` inputs | Open |
| TR-208 | Critical | Backend | PTS reserve/theta admin actions silently mutate global platform economic state even when the API call itself reports failure — no transaction wrapping around a shared singleton | Live-reproduced as 2FA-verified `VGC53` at `/admin/pts`: the `pts_components` singleton row (id=1) did not exist yet (`GET /admin/pts/components` was returning synthetic fallback defaults, including a documented `theta` default of `0.00005` — see `components_GET.xs` line 22). Submitted "Update R & A" with the *same* values already being displayed (`reserve_inr:0`, `hard_assets_inr:0`) purely to test the control — the UI showed only an error (`"Missing param: actor_admin_id"`, see TR-209) with no success indicator. **A direct Xano table read immediately after confirms the row was silently created for real**, with `updated_at` now a live timestamp — but with **`theta:0`, not the intended `0.00005` default**, because `reserve_assets_PATCH.xs`'s `db.add_or_edit` payload (lines 49-55) never includes a `theta` field, so the first-ever row creation falls back to the raw schema default (`0`) instead of the application-intended default. Root cause: `db.add_or_edit pts_components` executes and *commits* several steps before the `log_admin_action` call that actually throws (see TR-209) — there is no `db.transaction` wrapping the pair, so a downstream audit-logging failure does not roll back the real state change. **Net effect: the platform's global PTS conversion time-decay factor (θ) is now permanently `0`** (a value that, per the SRS's PTS formula, likely zeroes out or otherwise corrupts the time-decay component of every future PTS↔INR conversion for every member) — introduced by a no-op-intentioned test action, with no admin-visible confirmation that anything had changed. The identical unwrapped-mutation-before-audit-log shape recurs in `theta_adjust_POST.xs` (lines 26-31 vs. 34-46) and is architecturally probable in the other five `log_admin_action`-mismatched endpoints in TR-209. | Live reproduction 2026-07-20 at `/admin/pts`: `GET /admin/pts/components` before (synthetic defaults, `updated_at:null`) vs. direct Xano MCP table read of `pts_components` (table_id 845058) after submitting "Update R & A" (`theta:0`, `updated_at` now a real timestamp); `reserve_assets_PATCH.xs` lines 44-56 vs. `components_GET.xs` line 22 | Open |
| TR-209 | Critical | Backend | Systemic: 7 admin endpoints call the shared `log_admin_action` function with the wrong parameter names, hard-failing every one of these actions — including a DPDP/GDPR-style data-erasure compliance endpoint | `function/log_admin_action.xs` requires `actor_admin_id`, `action`, `target_type`, `target_id` (all non-optional). A `grep` across every caller found the overwhelming majority (30+ call sites) use the correct signature, but **7 call sites use one of two entirely different, non-matching parameter sets** and will always fail with `"Missing param: actor_admin_id"` (confirmed live for the first): (1) **`admin/admin/pts/reserve_assets_PATCH.xs`** — uses `admin_member_id`/`entity_type`/`entity_id`/`details` (live-reproduced, see TR-208 for the accompanying silent-mutation consequence); (2) **`admin/admin/pts/theta_adjust_POST.xs`** — identical wrong shape, same silent-mutation risk as TR-208; (3) **`admin/admin/pts/bootstrap_POST.xs`** — identical wrong shape; (4) **`admin/admin/courses/id/payout_POST.xs`** — uses `admin_id`/`action_type`/`notes` (missing `target_type`/`target_id` entirely); (5) **`admin/admin/courses/proposal_id/list_POST.xs`** — same wrong shape; (6) **`admin/admin/courses/amendments/id/decision_POST.xs`** — same wrong shape; (7) **`admin/admin/members/id/process_erasure_POST.xs`** — same wrong shape, and this one is the most serious: it anonymises a member's PII (`name`, `email`, `mobile`, `dob`, `city`, `state`, `country`, `avatar_url`, `device_fingerprint`, `registration_ip`) and marks the member's data-erasure request `completed` in two `db.edit` calls that both **commit before** the failing `log_admin_action` call — meaning a DPDP 2023 §17 erasure request would actually be carried out for real, but (a) the admin-facing API call reports failure, offering no confirmation the legally-required action succeeded, and (b) **no audit-log entry is ever created for a compliance-mandated erasure action**, which is itself a second, independent compliance gap. None of the 7 affected calls sit inside a `db.transaction`, so in every case the real mutation the endpoint exists to perform completes *before* the fatal error — the failure is purely cosmetic to the admin but substantively real underneath. Not live-reproduced for endpoints 3–7 this session (PTS bootstrap/theta_adjust source-confirmed identical to the live-reproduced reserve-assets case; courses/erasure endpoints source-confirmed only, deferred to avoid anonymising a fixture member without a dedicated erasure-request fixture). | Live reproduction 2026-07-20 at `/admin/pts` (`PATCH /admin/pts/reserve-assets` → `"Missing param: actor_admin_id"`); `grep -rn "function.run log_admin_action" -A 8 XANO/api` across all ~37 call sites, cross-referenced against `function/log_admin_action.xs`'s required-param signature | Open |
| TR-207 | Critical | Backend | Admin Marketplace "All Items" list is completely broken — join alias/table-name confusion crashes every call | `admin/admin/marketplace/items_GET.xs` (lines 37-49) declares a left join aliased `seller` against the `user` table (`seller: {table: "user", type: "left", where: $db.marketplace_items.seller_id == $db.user.id}`), then reuses the same wrong reference in `eval` (`seller_name: $db.user.name`, `seller_email: $db.user.email`). Xano's join `where`/`eval` clauses must reference the **join alias** (`$db.seller.id`), not the underlying table name (`$db.user.id`) — confirmed by the working sibling pattern in `marketplace/admin/orders_GET.xs` (lines 30-39), which correctly uses `$db.buyer.id`/`$db.item.id` for its own `buyer`/`item` aliases against the same `user` table. Because `user` is never actually joined under that name, the query engine cannot resolve the comparison and the whole query fails. Confirmed live as 2FA-verified `VGC53`: navigating to `/admin/market` immediately shows "Couldn't load this — `ParseError: Invalid value for param:\"marketplace_items.seller_id\"`" with a raw `400` on `GET /admin/marketplace/items` — the admin can never see, search, or manage any marketplace item through this screen; only the unrelated "Auto-Settle Orders" and "Resolve Dispute" controls above it render. | Live reproduction 2026-07-20 at `/admin/market` as 2FA-verified `VGC53`: `400 ParseError: Invalid value for param:"marketplace_items.seller_id"` on `GET /admin/marketplace/items`; `items_GET.xs` lines 37-49 vs. working `orders_GET.xs` lines 30-39 comparison | Open |
| TR-206 | Medium | Backend | Admin Token Surrenders list always displays "0 tokens" for every row — same `??`-against-a-non-null-default bug as TR-205, surfaced as a read-side display defect | `admin/admin/token_surrenders_GET.xs:39` maps the list response's `tokens` field with the identical `$s.tokens_to_surrender ?? $s.vgc_token_amount` expression, which — per TR-205's root cause — always evaluates to `0` since `tokens_to_surrender` defaults to `0` rather than `null`. Confirmed live in `/admin/declarations` → "Token Surrenders" tab as `VGC53`: every one of the 20+ listed surrender rows (across multiple members, multiple statuses) renders as "`#N — 0 tokens`" regardless of the real `vgc_token_amount` on the underlying record. This is the display-layer symptom of the same defect that makes TR-205 a financial bug on the write side — logged separately because the read-side (list display) and write-side (wallet debit) are two independent call sites that happen to share the same broken expression, and fixing one without the other would leave a half-fixed bug. | Live reproduction 2026-07-20 at `/admin/declarations` → "Token Surrenders" tab as 2FA-verified `VGC53`: all listed rows show "0 tokens"; `admin/admin/token_surrenders_GET.xs` line 39 source | Open |
| TR-180 | High | Frontend | Auth/cache — `SessionExpiredModal` re-login accepts a different account's credentials and resumes in place without clearing the TanStack Query cache | Quarantine-gate item 4. `SessionExpiredModal.tsx` (shown in-place on any mid-session `401`, per TR-136) has blank, unrestricted email/password fields and its `login()` mutation only calls the auth store's `setToken()` — unlike `ProfileScreen.tsx`'s explicit `logout()`, which calls both `clearToken()` **and** `queryClient.clear()`. Confirmed live: browser was on `/wallet` authenticated as `E2E Adult A`; a `window.fetch` patch forced the next Xano call to return `401`, correctly surfacing the modal in place (screen stayed mounted, matching the intended TR-136 behaviour); entering **`E2E Adult B`'s** distinct credentials in the modal (not Adult A's) was accepted without any warning or re-authentication-as-same-user check, and the Wallet screen resumed showing live data with no navigation. Source confirms the leak surface: `useMe()`/`useProfile()` and every `*-me` query (`courses-me`, `enrollments-me`, `investments-me`, `blog-me`, `blog-bookmarks-me`, `loans-me`, `contracts-me`, `expenses-me`, `activities-me`) use a **global, non-user-scoped** query key (e.g. `queryKey:['me']`), and `queryClient.ts` sets `staleTime: 30_000` with `refetchOnWindowFocus: false` — so any of these queries already cached for the outgoing account can legitimately keep rendering that account's private data to whoever is now authenticated for up to 30 seconds (longer if nothing triggers a refetch) after a same-tab identity switch through this specific path. The exact stale-frame was not captured on camera this run (both disposable test wallets are zero-balance, so a wallet-numbers flash wouldn't be visually distinguishable, and profile-name timing did not line up across the necessary round-trips) — logged on the strength of (a) live-confirmed reachability of the vulnerable path (arbitrary different-account login accepted, in-place resume with no cache clear) and (b) unambiguous source proof of the exact missing-invalidation mechanism and its blast radius. Recommend `queryClient.clear()` (or at minimum `removeQueries()` for every `*-me` key) alongside `setToken()` in `SessionExpiredModal`'s success handler, and scoping the affected query keys by the authenticated member id. | Live repro 2026-07-20 (`E2E Adult A`→`E2E Adult B` mid-session switch via `SessionExpiredModal`, local dev, forced 401 via `window.fetch` patch); source review of `SessionExpiredModal.tsx`, `ProfileScreen.tsx`, `api/queries.ts`, `lib/queryClient.ts` | Open |
| TR-179 | Critical | Backend | Auth — `suspended` flag has zero effect; suspended members keep full platform access on old and new tokens | Quarantine-gate item 3, **confirmed**. `authentication/login_POST.xs` contains no reference to `suspended` at all (`grep -n "suspend"` → zero matches) — password correctness is the only login gate, matching the already-known TR-137 pattern (no `email_verified_at` check either). Confirmed live end-to-end on disposable `VGC54`: captured a valid token, then set `suspended:true` via Metadata API, then (a) reused the **pre-suspension** token against `GET /me` → `200`, full profile returned including `"suspended":true` in the body itself (the account can see it is flagged, and nothing stops it acting anyway); (b) issued a **brand-new** `POST /login` with the correct password → `200`, a fresh valid token minted for an account already marked suspended. This means the admin "Suspend member" action (`AdminMembersScreen`/`admin/members/{id}_PATCH.xs` — itself reachable by anyone per TR-178) has no actual enforcement effect anywhere in the auth or session path; a suspended member retains full read/write platform access indefinitely on both old and freshly-issued tokens. Same root-cause family as TR-137 (deferred, email-verification gate absent) — recommend a single combined fix: check `suspended` (and ideally `email_verified_at`) in `login_POST.xs` and in a shared request-level guard, not just at the UI layer. **UI-level confirmation added 2026-07-20**: logged in as suspended `VGC54` through the real `/login` screen (local dev) — succeeded normally, landed on `/home` with the full authenticated shell (bottom nav, search, notifications) and no suspension banner/warning/restriction anywhere. | Direct `curl` reproduction 2026-07-20 against production Xano using disposable `VGC54`; `login_POST.xs` source grep; UI reproduction via `/login` browser flow | Open |
| TR-178 | Critical | Backend | Admin — `Quick Start/enforce_role` never actually denies "admin" access to anyone; **every** `enforce_role`-gated admin endpoint (~75+ files, essentially the entire admin control plane) is reachable by any ordinary logged-in member with zero admin flags at all | Discovered while investigating quarantine-gate item 2 (role/`role_flags` split-brain). `XANO/function/quick_start/enforce_role.xs` compares role levels via `$role_hierarchy|get:$user_role` where `$role_hierarchy = {admin: 2, member: 1}`. `authentication/signup_POST.xs` never sets the `role` enum field at all (only `role_flags.is_member`), so every ordinary member's `role` is the empty-string default — a key not present in the hierarchy map, so the lookup returns `null`. The gate's `if ($user_role_level < $required_role_level)` then evaluates `null < 2`, which does not trigger the `throw`, so the function returns success for **every** caller regardless of role. Confirmed live and reproducibly with a completely ordinary, never-admin-flagged disposable member (`E2E Adult B`, `VGC51`, `role:""`, `role_flags:{is_member:true}` only — no `is_admin` anywhere): a plain member bearer token successfully called `PATCH /admin/members/51` (self-edit, enforce_role-gated) returning `200` with the full raw member row (see TR-177), and `GET /admin/audit-log` (enforce_role-gated) returning `200` with the full 18-row platform admin action history. Unauthenticated and garbage-token calls to the same endpoints correctly returned `401`, ruling out a caching/CDN explanation — this is a genuine in-stack authorization defect, not a gateway or cache issue. `grep` of `Quick Start/enforce_role` call sites across `XANO/api` lists ~75 files gated this way, covering nearly the entire admin surface: member suspend/impersonate/erasure, wallet adjust/mint, PTS rate/theta/reserve/bootstrap, all financial admin actions (declarations/investments/sponsorships/donations), marketplace categories/items/order settlement/dispute resolution, blog moderation, contract dispute resolution (with fund release), loans, courses, elections, seasons, groups moderation, backup-admin designation, vacation mode, system config and admin reports. Combined with TR-176 (`require_admin.xs`/2FA bypass, a separate authorization path used by a smaller subset of admin endpoints), the platform's admin authorization is not enforcing at all for the `enforce_role`-gated majority. **No further admin mutation was attempted through this bypass beyond the two already-executed harmless no-op self-`PATCH`es (`suspended:false`, a value each account already had)** — per the quarantine-gate stop rule, all subsequent admin-panel coverage this run will go through the legitimate 2FA-verified `/admin/login` flow, and this finding is treated as confirmed rather than needing further reproduction. | Direct `curl` reproduction 2026-07-20 against production Xano using disposable `VGC51` (zero admin flags) and `VGC54`/`VGC53`; `enforce_role.xs`/`signup_POST.xs` source review; `grep` call-site inventory | Open |
| TR-176 | Critical | Backend | Admin — 2FA is fully bypassable; ordinary `/login` token grants complete admin API access | Quarantine-gate hypothesis §18 item 1, **confirmed**. Root cause: `XANO/function/require_admin.xs` authorizes solely on `$auth.role_flags.is_admin == true` from the bearer token's claims — it has no concept of *how* the token was minted. Both `authentication/login_POST.xs` (ordinary member login, no 2FA) and `admin/2fa/verify_POST.xs` (the OTP-gated flow) call the identical `security.create_auth_token` against the same `user` table with no distinguishing claim (no `mfa_verified`, no token scope). Confirmed live with the disposable `E2E Test Admin` (`VGC53`, promoted via Metadata API, 2FA never set up): a plain `POST /login` with just email+password returned a full auth token, which was then accepted by `GET /admin/audit-log` (200, full 18-row admin action history) and `GET /admin/members` (200, full raw member list — see TR-177). This makes the entire admin 2FA subsystem (OTP email, recovery codes, lockout) decorative: anyone who obtains an admin's password by any means (phishing, credential stuffing, the exposed historical password documented in TR-173's area, or the complete absence of any rate-limit/lockout on ordinary `/login` per TR-140) gets unrestricted admin API access with zero second factor. No admin mutation was performed with the bypass token per the quarantine-gate stop rule; all further legitimate admin-panel testing this run will go through the real `/admin/login` → 2FA flow. | Direct `curl` reproduction 2026-07-20 against production Xano using disposable `VGC53`; `require_admin.xs`/`login_POST.xs`/`admin/2fa/verify_POST.xs` source review | Open |
| TR-177 | High | Backend | Admin — `GET /admin/members` returns raw password hashes and other sensitive fields for every member | Same call as TR-176's reproduction: `GET /admin/members` returns the full `user` row per member including the `password` field (bcrypt-style hash string), `password_reset` token/expiration object, `device_fingerprint`, `registration_ip` and DOB — none of which are stripped, unlike `me_GET.xs` (fixed earlier this run's history to `\|unset` the password field for a member's own `/me`). Any admin session (2FA-verified or, per TR-176, not) can exfiltrate every member's password hash in one paginated call. Recommend the same `\|unset` treatment applied to `admin/members_GET.xs`. | Same `GET /admin/members` response captured for TR-176, response body inspected for field names only — no hash value was recorded or will be reused | Open |
| TR-175 | Medium | Frontend | Profile/Home/Wallet — own public Member ID never surfaced on the primary account screens | `ProfileScreen.tsx`, `HomeScreen.tsx` and `WalletScreen.tsx` never render `member_id` (confirmed by grep — zero matches for `member_id`/`memberId`/"Member ID" in all three files). A member's public Member ID (e.g. `VGC50`) is the identifier used platform-wide — Points transfer recipient lookup, Guardian Member ID at a minor's signup, marketplace/contract counterparty display — yet there is no obvious place in the product for a member to find their own ID. It is only incidentally visible as a read-only field on `DeclarationFormScreen.tsx`/`TokenSurrenderFormScreen.tsx` (fixed under TR-149 for a different reason: those forms submit it). Confirmed live: `GET /me` for disposable member VGC50 (E2E Adult A) returns `member_id:"VGC50"`; the rendered Profile/Home/Wallet DOM contains no reference to it anywhere. | Live `GET /me` response for `E2E-20260719-01` Adult A (VGC50) vs. Profile/Home/Wallet DOM snapshot and source grep, 2026-07-20 | Open |

## Existing defects reconfirmed

| Existing ID | Regression evidence |
|---|---|
| TR-138 | Terms and Privacy remain generic placeholders; opening them from a populated signup form loses all entered state. |
| TR-142 | Anonymous `GET /blog_dislikes` returned raw member-vote records. No mutation was attempted. |
| TR-146 | Login enumeration fix passes: unknown-email and known-email/wrong-password attempts show identical generic errors. |
| TR-163 | Still open, live-reconfirmed 2026-07-20: known email (`E2E Adult B`) → generic "If an account exists for..." success copy; unknown email → distinct `"No user found for that email."` alert. Enumeration still possible. |
| TR-168 | Production frontend response headers on `frontend-kappa-mocha-30.vercel.app/login` still show only `strict-transport-security`; no CSP, `x-frame-options`, `x-content-type-options`, `referrer-policy` or `permissions-policy`. |

## Accessibility and UX observations awaiting consolidation

- Login and signup have no `main` landmark.
- Login/reset errors and reset success are not exposed through `role="alert"`, `role="status"` or `aria-live`.
- Blank signup submission leaves focus on the submit button instead of the first invalid field.
- Required policy links replace the signup form in the same tab without preserving state.
