# E2E Defect Log — Run `E2E-20260723-01`

Summary of **new** confirmed defects found this run (full detail in
`TEST_REGISTER.md` Active Issues). New IDs start at **TR-260**. Regression
*confirmations* (previously-fixed items verified still working) are recorded in
`E2E_COVERAGE_LEDGER.md`, not here.

| TR | Severity | Layer | Area | One-line | Status |
|---|---|---|---|---|---|
| TR-265 | Critical | Backend | Token surrender creation (wallet lookup) | `create_POST.xs` compared `wallets.type == "VGC_TOKEN"` against the real lowercase stored value `"token"` — a 6th, previously-uncaught wallet-casing instance (TR-181/191/205/227/232/240 family). Every wallet lookup returned null, 404ing "Token wallet not found" for every member regardless of balance. Found via negative-path testing of TR-263's fix (no real record created). | **Fixed & verified 2026-07-24** — literal `"token"`; request now reaches the real balance precondition. |
| TR-264 | High | Backend | Token surrender lifecycle — UPI ID never persisted | `upi_id` is collected by the FE form but has no column anywhere in `token_surrenders` (or any other table) to be saved to — an admin completing a real surrender has no system-recorded destination for the INR. Needs a schema/product decision. | **Open** — awaiting decision on where UPI should live. |
| TR-263 | Critical | Frontend | Token surrender creation (field-name mismatch) | `createSurrender` sent `{tokens, upi_id}`; the live endpoint requires `{vgc_token_amount, conversion_rate}`. Every real submission 400'd before any business logic ran — WF-05 was completely non-functional end-to-end. Found via negative/boundary-only write-flow testing (no real record created; VGC75's 0 token balance guarantees the precondition blocks completion either way). | **Fixed & verified 2026-07-24** — correct field names + the form's already-computed rate now sent as `conversion_rate`. |
| TR-262 | Medium | Frontend | `/explore` marketplace list | `MarketplaceScreen` price hardcoded `formatAmount('VGC_TOKEN', …)`, so INR and VGC_POINTS items were all mislabeled "Tokens" (live catalog is a real mix). Found while browsing `/market/item/11` (₹22.91 INR) and cross-checking the list. Detail screen was already correct → list/detail disagreed. | **Fixed & verified 2026-07-23** — use `item.currency`; list now shows ₹/Tokens/Points correctly. |
| TR-261 | High | Frontend | `/expenses` Entries | `ExpensesScreen` renders `expense.amount` but the real field is `amount_inr` → every expense row shows ₹0. Live-reproduced (VGC75, two real expenses ₹250 + ₹150.50 both show ₹0). Read-side of the amount/amount_inr family. | **Fixed & verified 2026-07-23** — read `amount_inr` (+ `settlement_status`); browser now renders ₹250 and ₹150.5 (no ₹0). |
| TR-260 | High | Frontend | `/loans` My Loans | `getMyLoans` unwraps `.items` but `/loans/me` returns `{loans}` → member's loans list always empty in the UI (Repay unreachable). Live-reproduced (VGC75, loan id 1 present in API, "No loans" in UI). Isolated: a sweep of the other member list endpoints confirmed they all correctly return `{items}`. Bundled: member LoanCard also has `amount`/`outstanding`/status-casing display bugs. | **Fixed & verified 2026-07-23** — `getMyLoans` unwraps `.loans`; LoanCard reads `amount_inr`/`outstanding_tokens` and lowercase status (`active`) with a `STATUS_LABEL` map; browser now shows the ₹2,000 Pending loan (no "No loans"). |
| TR-257 | Critical (raised from High) | Both | `/financial/investments|sponsorships/new` | Logged-in member cannot create an investment/sponsorship via the UI — the form only shows the email field to anonymous users, but the backend (no optional-auth) demands `investor_email`, dead-ending with "unauthenticated submissions". Live-reproduced in browser 2026-07-23 (VGC75, ₹1500, Option A). Severity raised after UI confirmation. | **Fixed & verified 2026-07-23** — both endpoints moved to `auth = "user"`, `member_id` from `$auth.id`, email-gate removed (forms are `RequireAuth`-only, so the anonymous branch was dead code). Live: no-token → 401; token + non-positive amount → the positive-amount precondition (email dead-end gone). Deployed live + synced. |
