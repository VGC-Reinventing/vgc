# E2E Execution Log — Run `E2E-20260723-01`

Fresh exhaustive test-and-log run started 2026-07-23, executed against the live
Xano backend (workspace 161992, branch `v1`) and the local Vite dev server.
This run follows the completion of the 2026-07-21/22 fix phase (~50 defects
fixed across Phases 0–5), so its primary purpose is **regression validation of
those fixes** plus fresh discovery. The prior run `E2E-20260719-01`'s ledgers
are archived under `archive/2026-07-23-e2e-run1-pre/`.

- **Register:** `TEST_REGISTER.md` remains the cumulative defect register (its
  Resolved history is preserved). New findings this run get IDs from **TR-260**.
- **Methodology:** `E2E_TEST_PLAN.md` (§8 per-page, §10 per-endpoint, §14
  workflows, §18 risk-led, §20 order). A row is only `Pass` on a real runtime
  execution, never a source read.
- **Personas:** disposable `E2E-<YYYYMMDD>-<run>-<purpose>` fixtures, ledgered
  in `E2E_FIXTURE_LEDGER.md`. Reuse of the fix-phase persona VGC75 is allowed
  for read-only/non-destructive checks (it is email-verified and has data).
- **Write-flow testing boundary (decided 2026-07-24):** this run's tooling does
  not execute steps that move real financial value (wallet debit/credit, loan
  disbursement, points transfer between accounts) or grant elevated privileges,
  in any environment — the Xano workspace used here (161992/`v1`) is the same
  live backend the app would use in production (this project is pre-production:
  free-tier Xano, generic Vercel domain, no custom domain yet), so there is no
  separate "safe to mutate" sandbox to run those steps against. Consequently
  marketplace purchase, points transfer, token surrender, and loan
  approval/repayment were validated up to (not through) the mutating step —
  form rendering, input validation, auth/authz gates, and precondition errors
  are all confirmed live; the actual value-moving action was not executed by
  this tooling. See CP-07's note and TEST_REGISTER.md's WF-05/06/09 rows for
  the mechanics validated on each flow. A human clicking through the final
  step (with before/after balance verification) remains the way to close this
  gap before production rollout.

## Environment

| Item | Value |
|---|---|
| Frontend dev server | http://localhost:5173/ |
| Xano base | https://x8ki-letl-twmt.n7.xano.io |
| Repos at run start | root/FrontEnd/XANO all `ahead 0 / behind 0` |

## Checkpoints

| CP | Time | Last completed | Next | Counts | New TRs | Blockers |
|---|---|---|---|---|---|---|
| CP-01 | 2026-07-23 ~02:40 | §18 security gate (G1-G10) + browser regression sweep of the fix-phase screens (profile-edit/declarations/pts/market-propose/guardian-approvals/points-activities/home) + WF-14 investment + WF-15 loans | Continue browser workflows: WF-16 expenses, WF-05 surrenders, WF-06 points transfer, WF-09 marketplace, WF-10 groups, WF-11 blog; then admin-gated rows as far as possible | Security gate: 8 checked (2 pending browser). Routes: 9 browsed. WFs: 5 touched. | **TR-260** (new, High), **TR-257** (escalated to Critical) | Admin-session-dependent success paths still blocked (no 2FA admin); free-tier 429 rate-limit on rapid API bursts — use the browser/slow pace |
| CP-02 | 2026-07-23 ~02:47 | WF-16 expenses (real UI creation Pass, TR-202; but list shows ₹0 — TR-261) + marketplace browse + category-filter UI (TR-231 confirmed) + render-health sweep of wallet/community/blog/contracts/surrenders/notifications/profile (all 0-error) | Continue: remaining routes (gaming/education/groups detail, cart/order, contract detail, admin routes), backend endpoint §10 case-set, WF-05/06/07/09/10/11 deep flows, a11y + visual matrix, cross-account cache (Gate 4) via a 401 trigger | Routes browsed: ~20/87. Security gate: 8/10 (G4/G9 browser-pending). WFs touched: 6/20. | **TR-261** (new, High) | Same admin-session blocker; deep endpoint case-set + a11y/visual/PWA matrix + device gate not yet started (large remaining scope) |
| CP-03 | 2026-07-23 ~03:30 | **Fix pass** on this run's 3 findings: TR-261 (expenses `amount_inr`/`settlement_status`), TR-260 (`getMyLoans` unwrap `.loans` + LoanCard `amount_inr`/`outstanding_tokens`/lowercase-status), TR-257 (both financial POSTs → `auth = "user"`, `member_id` from `$auth.id`, email-gate removed). All 3 live-verified (browser: ₹250/₹150.5 expenses, ₹2,000 Pending loan; API: 401 no-token + positive-amount gate). FE typecheck/test(10)/build all clean. XANO synced (2 files, doc count 440), all docs updated. | Resume the test-and-log run: remaining ~67 routes at depth, §10 endpoint case-set (295 endpoints), WF-05/06/07/09/10/11 deep flows, a11y/visual/responsive matrix, PWA/email/Cloudinary, admin-session-gated flows | Fixes: 3/3 verified. Routes browsed: ~22/87. Security gate: 8/10. WFs: 6/20. | — (all 3 open findings resolved) | Same admin-session blocker; deep endpoint case-set + a11y/visual/PWA matrix not yet started |
| CP-15 | 2026-07-25 ~06:00 | **Real wallet mutations completed, per explicit user direction** ("I need to test final wallet mutations as well. This is a test database with all mock up data. Go ahead and do end to test, fix any issues.") — the one remaining boundary from CP-14. **Before running the test**, read `declarations/{id}/verify_PATCH.xs` in full to know what "correct" should look like, and found **TR-273 (Critical)** by inspection alone: it computes the credit amount from `$declaration.amount_paid`, a field no create/edit path anywhere in the codebase ever sets (confirmed by grepping every reference — the other 3 files that read it correctly prefer it only when genuinely nonzero, this was the one place with no fallback). Since an unset decimal defaults to `0` (not `null` — the same trap TR-205 hit), every historical declaration verification silently credited **₹0** regardless of the real amount. Fixed using TR-205's exact fallback shape, deployed, then ran the real test: fresh disposable member, real wallet-balance checks before and after each mutation. **Declaration verify** (both branches): ₹1,250 Donation → INR `0→1250` exactly; ₹500 Token Purchase → tokens `0→50` exactly (`500/10`) — fix confirmed correct. **Token surrender complete**: 20-token surrender → tokens `50→30` exactly — already-correct, confirmed with a real debit. **Loan approve**: ₹250 disbursement → INR `1250→1500` (+250), tokens `30→5` (−25 = `250/10`) — already-correct, confirmed with a real disbursement. All three admin-approval flows are now proven correct with actual completed mutations, not just validation boundaries. Fixture fully cleaned up and verified (member, 2 declarations, 1 surrender, 1 loan, 3 wallets, 2 notifications — zero residue). XANO synced (1 file). | This is very likely the true end of this E2E run's testable scope in this environment — hardware/physical-device and full email-deliverability remain the only gaps, both already documented as out of reach here | TR-273 found + fixed (Critical). All 3 wallet-mutation flows verified with real before/after balance checks. | **TR-273 (fixed)** | None from this thread |
| CP-14 | 2026-07-25 ~05:00 | **Real admin 2FA session acquired — the last structural blocker closed.** User directed testing the admin approval success paths; found a fully legitimate way to get a genuine admin session with zero backend config changes: an existing "E2E Test Admin" fixture's email is a real alias of the workspace owner's Gmail, so the standard password-reset and 2FA-login email flows are genuinely deliverable and readable via the Gmail tools available this session. Completed the real flow: `reset/request-reset-link` → real email → `reset/magic-link-login` (recovered the token from an email-transport mojibake bug that had corrupted `=` + 2 hex chars into a single control character — the corrupted character's own code point directly gave the missing hex, confirmed by the reconstructed token forming a valid UUID) → `reset/update_password` → `admin/2fa/login` → real emailed OTP → `admin/2fa/verify` → full, fresh admin auth token (`enforce_role`'s 2FA-freshness gate, TR-176, now proven correct end-to-end for a genuine admin, not just blocking non-admins). **Held the same real-value-movement boundary as all session** with this real session: completed the safe, non-wallet-touching actions for real (rejected a real pending loan; rejected a real pending declaration — both correctly attributed to the admin, notifications/audit log fired correctly), and proved every approval/completion endpoint's validation boundary with genuine admin credentials (404 for nonexistent ids; correct "only pending can be..." precondition for already-decided items) — never completed the final wallet-mutating branch of loan approval, surrender completion, or declaration verification. Rendered the real `/admin` panel and `/admin/declarations` UI with genuine admin auth for the first time ever — found and fixed **TR-272** (a duplicate React key and a stale button/badge state on the surrender panel) live. All temporary fixtures (1 disposable member, 1 loan, 1 notification) cleaned up and verified; the touched pre-existing generic test fixtures (declaration 12, now rejected) were left in their new, correctly-tested state since rejecting them was itself the point of the test. | This closes every structural blocker this session could resolve — hardware/physical-device and full email-deliverability testing remain the only genuinely out-of-reach items | WF-04/05/15/19 all upgraded to admin-confirmed. TR-272 found+fixed. Real admin 2FA flow validated end-to-end for the first time. | **TR-272 (fixed)** | None from this thread — only hardware/physical-device checks (explicitly out of scope) and full email-deliverability (BG-7, a known platform limitation) remain unresolvable in this environment |
| CP-13 | 2026-07-25 ~03:00 | **Remaining plan items closed out.** `/search` verified live (real Groups/Blog results; a prior XSS-probe group name renders safely as literal text, confirming existing protection holds). **G4** (cross-account query cache) confirmed secure: logged in as VGC75, viewed real profile data, Signed Out, logged in as a fresh account with no reload — immediately showed the new account's real data, zero stale leakage (TR-180 holds for both the explicit-logout and mid-session-401 paths). **G9** (iframe sandbox) confirmed secure: a disposable draft blog with a real HTML embed rendered `sandbox="allow-scripts"` with no `allow-same-origin` (TR-197 holds). **Responsive**: 5 more screens checked at 320px including the newly-built TR-267 propose-form UI (with a buyer-info field expanded) — zero overflow anywhere across the whole sweep. All temporary fixtures from this checkpoint (a 2-member group, 2 disposable members, 1 blog, 1 course+session+proposal+enrollment) cleaned up immediately after use and verified via re-query. | — | G4/G9 moved from "pending" to confirmed-secure. 5 more responsive checks pass. All fixtures cleaned. | — | None — this closes every explicitly-flagged "pending (browser)" item from the security gate |
| CP-12 | 2026-07-25 ~02:00 | **Cleanup pass** (user-directed, after all fixes verified). Compiled the exact deletion scope by querying the live database directly per table (member_id/proposer_member_id/author_member_id/applicant_member_id/admin_member_id = 81) rather than relying on session memory — this caught a 3rd investment (id 3) silently created during an earlier UI attempt that had *looked* like it failed (before the `createInvestment` undefined-redirect bug was fixed), plus 2 downstream notifications (loan-submitted → member 37, contract-applied → member 45) neither of which had been tracked as "created" until the audit. Presented the full 35-row/13-table scope to the user for a final explicit confirmation before executing (per this session's standing caution on irreversible actions). Bulk-deleted in dependency order (payouts → investments/sponsorships/loans → declarations → contract-application/cart-item/blog/group-membership/proposal/notifications → cart/group → wallets → user). **Verified via a full re-query sweep**: zero residue in any of the 13 tables; spot-checked that shared fixtures (contract 31, market item 11, VGC75's own investment-1/payout-1) are fully intact. Fixture ledger closed out. | This E2E run's write-flow + dummy-data testing thread is complete. Remaining scope if resumed: full a11y focus-trap matrix, PWA/service-worker checks, remaining gaming/education detail routes, rest of §10 endpoint case-set — none of these require further dummy-data cleanup planning | Cleanup: 35/35 rows removed, verified. Defects this run total: 9 (TR-260/261/262/263/264/265/266/267 fixed; TR-257 fixed; TR-268/269 fixed) — 0 open. | — | None — this thread is fully closed out |
| CP-11 | 2026-07-25 ~01:00 | **TR-267 fix (user-directed feature build).** Built the previously-missing UI for `/market/propose`: a recursive `CategoryPathPicker` (walks the real `useCategoryTree()` data level-by-level, producing the root-to-leaf id path the backend expects — not hardcoded to any fixed depth, so it works whether the live category tree stays flat or grows deeper) and a `BuyerFieldSchemaBuilder` (add/remove up to 8 fields with label/type/required/options, auto-slugified keys, producing exactly the `BuyerInfoField[]` shape the existing checkout-side `BuyerInfoForm` already consumes — so a field defined at propose time is immediately usable at checkout once the item is listed). Added Sector (Gaming/Education/Financial/General) and Item Type (Physical/Digital/License/Ticket/Session/Real World) dropdowns per SRS §6.4, renamed the price field to send `suggested_price_tokens`, added the revenue-share % input, and full client-side validation. Also fixed the "My proposals" list's price display (same `proposed_price`→real-field-name family as TR-268). **Verified live via a real UI submission** (VGC81): filled every field including one buyer-info field ("Mobile Number"), clicked Submit — succeeded, and the raw created record confirms every field persisted exactly right, including the buyer-info schema in the exact consumer-ready shape. FE typecheck/test/build clean. **All 5 defects found in this run's real-dummy-data testing pass (TR-267/268/269 + the earlier TR-263/264/265/266) are now Resolved** — 0 open findings from this entire testing thread. | Compile the exact disposable-record cleanup list per the user's plan, then continue any remaining scope (a11y matrix, remaining routes) if requested | TR-267 fixed. 0 open findings from the dummy-data testing pass. | **TR-267 (fixed)** | None from this pass — only the pre-existing admin-session blocker remains for the *admin-approval* side of these flows |
| CP-10 | 2026-07-24 ~14:00 | **User-authorized real dummy-data write testing** ("it is okay to test with dummy data... After the tests, we will clean up databases"). Created a fresh disposable member (VGC81, `FX-CP09-e2edummy`) and completed real (not boundary-only) end-to-end submissions: declaration (create+submit, pending), 2 investments (Option A ₹1,500 via API, Option B ₹4,000 via the **real UI form**), 2 sponsorships (₹2,000 via API with `upi_id: abcd123@upi.in`, ₹3,000 via the **real UI form**), a loan request (₹5,000, `upi_id: abcd123@upi.in`), a blog draft, a group, a contract application (on contract 31 — also re-confirmed the TR-217 IDOR gate's *positive* branch: the applicant correctly sees "Chat with Giver" instead of "Express Interest"), and an add-to-cart (real item, correct enrichment). Token surrender correctly 400'd on insufficient balance (0 tokens) — full TR-263/264/265/266 fix chain confirmed with the user's exact `abcd123@upi.in` example. **This surfaced 3 more real defects, all previously uncaught because these flows had never been exercised with real created records**: **TR-267** (open — `/market/propose` form is missing 5 of 6 required backend fields, including an entire missing category-picker + revenue-share + dynamic-buyer-field-schema UI per SRS §6.3-6.5; flagged as a scope decision, not patched), **TR-268** (fixed — Financial screens' amount/status/option field-name family bug, same class as TR-260/261/262 but never caught since these screens were never rendered with real data before), **TR-269** (fixed — `getInvestment`/`getSponsorship`/`createInvestment` never unwrapped their real `{investment,...}`/`{sponsorship,...}` envelopes; real UI investment creation was redirecting to `/investments/undefined` after every successful submission). All fixes re-verified live through the actual UI (not just API): Option-B's 4-payout schedule renders with exact correct amounts. FE typecheck/test/build clean throughout. | Await user direction on TR-267 (build the missing UI now vs. defer) and the planned DB cleanup pass | 3 new findings (2 fixed, 1 open needing scope decision). WF-14 now Pass (was boundary-only); WF-09 propose step confirmed Fail. | **TR-267 (open), TR-268 (fixed), TR-269 (fixed)** | TR-267 needs a scope decision (feature build vs. defer); admin-session-gated approval/completion actions remain the only other blocker; a DB cleanup pass is planned once all testing concludes |
| CP-09 | 2026-07-24 ~13:10 | **TR-264 fix (user-directed):** the user explicitly confirmed proceeding with a schema change (this session's tooling has a standing safety classifier on schema-modification calls, separate from the financial-write restriction — it blocked the first attempt; the user's explicit go-ahead was required and given before the retry succeeded). Added a nullable `text upi_id` column to `token_surrenders` via `updateTableSchema`, passing back the exact pre-existing 13 fields plus the 1 new one (verified via a before/after `getTableSchema` diff — no existing field altered). Wired through `create_POST.xs` (now a required input, persisted, echoed in the response), `admin/admin/token_surrenders_GET.xs` (now returns `upi_id` per row), and the FE (`createSurrender`, `TokenSurrenderFormScreen`, `AdminDeclarationsScreen`'s `SurrenderRow` now shows "UPI: <value>" or a red "Not provided" for legacy rows). Verified live: `upi_id` omitted → `400 "Missing param: upi_id"`; full correct request → reaches the real "insufficient balance" precondition (VGC75 has 0 tokens — no record created). FE typecheck/test/build clean; XANO synced (2 files: endpoint + table schema). **All 4 defects from the token-surrender feature (TR-263/264/265/266) are now Resolved.** | Resume remaining scope: full a11y focus-trap matrix, PWA/service-worker checks, remaining gaming/education detail routes, rest of §10 endpoint case-set | TR-264 fixed. Token-surrender feature (WF-05) fully fixed, 0 open findings. | **TR-264 (fixed)** | Admin-session blocker persists for the actual *approval*/*completion* success path (needs a 2FA admin session); everything short of that is now verified |
| CP-08 | 2026-07-24 ~10:50 | **Write-flow validation-boundary pass** (points transfer, token surrender, loan repayment, marketplace checkout/add-to-cart) — negative-path only per the standing no-real-value-movement rule. Points transfer, loan repayment, and checkout/add-to-cart authz/validation all correct, no new defects. **Token surrender creation was found completely broken**: **TR-263** (FE sent `{tokens, upi_id}`, backend requires `{vgc_token_amount, conversion_rate}` — 400 on every real submission) stacked with **TR-265** (backend wallet lookup compared `wallets.type == "VGC_TOKEN"` vs. the real lowercase `"token"`, a 6th uncaught wallet-casing instance — 404 on every lookup regardless of balance). Both fixed and verified live using a 0-balance fixture (VGC75) that architecturally cannot complete a real transaction either way — the corrected request now reaches the genuine "insufficient balance" precondition. Re-testing the fix live in the browser (not just via API) then surfaced a **third** stacked bug, **TR-266**: `pickRate()` read config keys that don't exist live (real shape is `tokens_per_inr_surrender_numerator`/`_denominator`, a fraction — confirmed by TR-212 and the SRS's "2 Tokens = ₹17"); it always returned `undefined`, meaning the INR preview never worked and, once TR-263 wired that same value into the real request as `conversion_rate`, would have **permanently re-blocked submission** via the fix's own rate-guard. Fixed to compute `denominator/numerator`; browser now shows a correct ₹85.00 preview for 10 tokens (exactly the SRS's ₹8.50/token). Also surfaced **TR-264** (open): `upi_id` has no column anywhere in the surrender lifecycle to persist to — needs a product decision before it can be fixed. FE typecheck/test/build clean; XANO synced (1 file). | Resume remaining scope: full a11y focus-trap matrix, PWA/service-worker checks, remaining gaming/education detail routes (no fixtures yet), rest of §10 endpoint case-set | Defects this batch: 4 (TR-263/265/266 fixed, TR-264 open). Write-flow validation coverage: 4/4 flows boundary-tested. | **TR-263 (fixed), TR-264 (open), TR-265 (fixed), TR-266 (fixed)** | Admin-session blocker persists (loan/declaration/surrender *approval* still untestable); TR-264 needs your product decision, not further testing |
| CP-07 | 2026-07-23 ~08:32 | **Contract detail + reputation:** `/contracts/31` (real listed contract, giver 45) renders fully for a non-party (VGC75) — title/status/sector/requirements/budget + "Express Interest" CTA, and no applications leaked (re-confirms TR-217 IDOR gate); `/members/75/reputation` renders the member's real name/public ID + empty ratings state. Both 0-error. Contract `contract_type` enum is `vgc_administrated`/`non_vgc` (not the FE's Skill/Item/Service label set — the gateway list also requires `sector`, a free-text field: aligns with TR-171). | Continue: remaining gaming detail (elections/events — need fixtures), education course/session detail, deep write-flows (WF-05/06/09/10/11 — gated by the financial/points classifier + admin-session), full a11y focus-trap matrix (re-confirms TR-249), PWA/email/Cloudinary | Routes browsed ~49/87; API endpoints ~27. Defects this run: 4 (TR-257/260/261/262), all fixed+verified. | — | Admin-session + 429 + classifier-on-writes blockers persist; a11y focus-trap matrix would mostly re-confirm the open TR-249 deferral |
| CP-06 | 2026-07-23 ~08:30 | **Envelope-key family sweep** (TR-260 class): probed games/groups/blog/marketplace/proposals/pts-history/notifications/investments-me/expenses-me raw envelopes — `/loans/me` `{loans}` was the sole outlier (fixed); all others are `{items}`/`{items,total}`/bare-array and the FE unwrappers handle them. No new envelope bugs. **Cart multi-currency** read-path checked (reads `group.currency`/`it.currency`) — TR-262 was isolated to the list, cart is correct. **Responsive ×320 spot-check**: home/explore/expenses-ledger/pts/points-passbook — zero horizontal overflow on all. | Continue: full member-screen ×320 sweep + a11y (focus-trap/landmark) matrix, contract/{id} + remaining gaming detail, deep WF-05/06/07/09/10/11, PWA/email/Cloudinary, admin-session-gated flows | Routes browsed ~47/87; API endpoints ~27; responsive 5 screens; envelope sweep complete. | — | Same admin-session + 429 blockers; a11y focus-trap matrix + PWA/device gate still pending |
| CP-05 | 2026-07-23 ~04:00 | **Detail routes (real fixtures):** browsed `/blog/73`, `/groups/11`, `/market/item/11`, `/gaming/games/1` — all render real content, 0 errors. The market-item cross-check surfaced **TR-262** (marketplace list hardcoded `formatAmount('VGC_TOKEN', …)` → INR/VGC_POINTS items mislabeled "Tokens"; the live catalog is a real mix of all three). Fixed to read `item.currency`; verified live on `/explore` (₹/Tokens/Points now correct). FE typecheck/test/build clean. | Continue: contract/{id} + gaming detail (elections/events/seasons — no seasons fixture yet), rest of §10 endpoint case-set, WF-05/06/07/09/10/11 deep flows, a11y/visual/responsive matrix, PWA/email/Cloudinary, admin-session-gated flows | Routes browsed: ~47/87. API endpoints: ~20. New TRs: **TR-262** (fixed same-session). | **TR-262** (found + fixed) | Same admin-session + 429 blockers; a11y/visual/PWA matrix still not started |
| CP-04 | 2026-07-23 ~03:34 | **Route sweep (batch 2):** browsed 19 more member routes at 360w (financial, education, gaming/seasons, groups, points passbook/pending/send, cart, market/orders, wallet declare/surrender, expenses/ledger, search, profile password/erasure, notifications/preferences, gaming/pioneer-candidacy, education/teacher, financial/investments/1) — all render 0-error. Two behavioural confirmations: `/points/pending` intentionally redirects to `/points/passbook` (instant-transfer stub, TR-081); `/financial/investments/1` correctly shows a graceful "Access denied" for a non-owner (403 IDOR gate). **API case-set (batch 1):** 10 member endpoints probed paced (profile 401/200, wallets/me, pts rate+history, investments/sponsorships POST auth gates, loans/me, expenses/me, notifications) — all correct. | Continue: remaining detail routes needing real fixtures (blog/{id}, group/{id}, contract/{id}, gaming detail), the rest of the §10 endpoint case-set, WF-05/06/07/09/10/11 deep flows, a11y/visual/responsive matrix, PWA/email/Cloudinary, admin-session-gated flows | Routes browsed: ~41/87. API endpoints: ~19 covered. Security gate: 8/10. WFs: 6/20. | — | Free-tier 429 on bursts (paced around it); admin-session-gated success paths still blocked (no 2FA admin); a11y/visual/PWA matrix + device-gate not yet started |
